var documenterSearchIndex = {"docs":
[{"category":"section","location":"field_mode/#Field-Resolved-Mode","page":"Field-Resolved Mode","text":"By default ModelPNPS propagates a complex envelope: an analytic field about a carrier, on Luna's Grid.EnvGrid. That is the standard choice and the right one for almost every run. build_setup(; field_mode = true) instead propagates the real, carrier-resolved field on a Grid.RealGrid.\n\nThis is a diagnostic, not a production default. It costs roughly twice the memory and three times the time per delay point, and it exists to answer one question: how much of a residual belongs to the envelope approximation rather than to the retrieval.","title":"Field-Resolved Mode"},{"category":"section","location":"field_mode/#Why-it-exists","page":"Field-Resolved Mode","text":"At 260 nm a 1 fs pulse is 1.15 optical cycles. The envelope approximation is marginal by construction at that duration, and the production envelope grid's relative-frequency window has its red edge at exactly DC — so there is no headroom left to argue with. When a retrieval leaves an unexplained residual at the 10⁻³ level, \"the forward model is an envelope model\" is a live hypothesis, and the only way to test it is to run the same geometry in both representations and difference the traces.\n\nThat test has been run. The reference paper (see The paper) repeated 16 delay points of its 1 fs production scan in field-resolved mode and measured an envelope-versus-field trace difference of 12times10^-5 at the 9.5 µm substrate — a factor of about 400 below the model residual under investigation there, which exonerated the envelope representation. The mode exists to make exactly this kind of statement possible.\n\nThree things differ on a real grid:\n\nthere is no carrier/envelope split — the field is the field;\nthe third-harmonic term is not dropped, nor are the counter-rotating terms;\nthere is no negative-frequency wrap, because the spectrum is the monotonic rfft half-spectrum of a real field rather than an FFT-ordered window about a carrier.\n\nEverything else is unchanged. The masks, windows, delay ramp, k-space builders and the whole extraction path are written against the absolute frequency axis grid.ω, which both grid types carry, so none of them branch on the representation.","title":"Why it exists"},{"category":"section","location":"field_mode/#Using-it","page":"Field-Resolved Mode","text":"setup = build_setup(;\n    λ0 = 260e-9, τfwhm = 1e-15, energy = 0.1e-6,\n    thickness = 40e-6, material = :SiO2,\n    mask_diam = 1.0e-3, mask_spacing = 0.5e-3,\n    λlims = (143e-9, 600e-9),\n    beam, window,\n    field_mode = true,           # Grid.RealGrid\n    response = :nothg,           # the default; see below\n    ffac = 6,                    # the default; see below\n)\n\nTwo further keywords are only meaningful when field_mode = true.","title":"Using it"},{"category":"section","location":"field_mode/#response-—-which-nonlinearity","page":"Field-Resolved Mode","text":":nothg (also :auto, the default) — the polarisation tfrac34varepsilon_0chi^(3)E_a^2 E, via Luna.Nonlinear.Kerr_field_nothg.\n:thg — the polarisation varepsilon_0chi^(3)E^3, via Luna.Nonlinear.Kerr_field.\n\n:nothg has the same physics content as the envelope Kerr_env, evaluated on a carrier-resolved field. That is precisely what makes it the default: a comparison made with it isolates representation error with nothing else changed. :thg adds the third-harmonic and counter-rotating terms, so the difference between the two runs is exactly what the envelope omits.\n\nnote: What `:thg` does and does not propagate on a UV window\nThe third harmonic is generated on the fine grid and then discarded by the crop back to the propagated grid whenever it falls outside the window. At λlims = (143, 600) nm the 3ω band of a 2 fs 260 nm pulse starts above ωmax, so none of it survives. What is retained are the within-band counter-rotating terms, and those are the real difference from the envelope. To propagate the third harmonic itself, extend λlims down to about λ0/3.","title":"response — which nonlinearity"},{"category":"section","location":"field_mode/#ffac-—-the-nonlinear-grid-sampling-factor","page":"Field-Resolved Mode","text":"ffac is forwarded to Grid.RealGrid and sets how finely the nonlinear grid is sampled relative to the propagated one.\n\nffac = 6 (default) sizes the fine grid for E^3.\nffac = 4 is enough for :nothg alone, and typically removes the oversampling entirely — halving both memory and per-step cost. Luna's ffac convergence for the no-THG response testset measures 4 × 10⁻⁸ agreement, unchanged from z = 0 to z = end.\n\nwarning: `ffac` changes the grid\nIt changes δω, the realised time window and in general Nω, so an ffac = 4 trace is not bin-for-bit comparable with an ffac = 6 one, nor with an envelope file. Use it for a self-contained scan, not for the comparison the mode exists for — and only with a convergence check against the default.","title":"ffac — the nonlinear-grid sampling factor"},{"category":"section","location":"field_mode/#Cost","page":"Field-Resolved Mode","text":"Field mode is much heavier than the envelope, and in a way the old \"ten times the field size\" rule of thumb does not capture: the state is twice as long in ω, the nonlinear evaluation runs on a grid twice as long again in time, and the no-THG response carries a complex analytic-signal buffer on that grid.\n\nMeasured device budgets at the production shape (N = 768, 40 µm substrate):\n\nConfiguration Nω Device Host peak\nenvelope 256 ~24 GiB ~12 GiB\nfield :nothg, ffac = 6 513 ~92 GiB ~25 GiB\nfield :thg, ffac = 6 513 ~65 GiB \nfield :nothg, ffac = 4 513 ~65 GiB \n\nSo :nothg at ffac = 6 is H200-only; :thg and :nothg-at-ffac = 4 fit an 80 GB H100; an A40 fits none of them. At N = 1024, :nothg needs 164 GiB and fits nothing currently available.\n\nIn time, field mode costs roughly 3× the envelope per delay point at matched step counts (measured 3.0× at N = 64 and 3.3× at N = 128).\n\nCall memory_budget before launching — it accounts for the response's analytic-signal buffer, which is allocated lazily on the first right-hand-side evaluation rather than at setup, and so is invisible to a measurement taken after build_setup alone. See Running on a GPU.","title":"Cost"},{"category":"section","location":"field_mode/#Raman-is-refused","page":"Field-Resolved Mode","text":"raman = true with field_mode = true throws an ArgumentError. The reason is given in Nonlinear Response: the nuclear fraction f_R is defined in the envelope convention, and that definition does not carry over to a carrier-resolved field without its own derivation and consistency test.","title":"Raman is refused"},{"category":"section","location":"field_mode/#Reading-field-mode-output","page":"Field-Resolved Mode","text":"A field-mode run writes a field_mode marker into the file's /grid group, and /grid/ω is then the monotonic rfft half-spectrum rather than the FFT-ordered relative-frequency axis an envelope run writes.\n\nload_simulated_scan reads the marker and does the right thing: it fftshifts an envelope file into natural order and leaves a field-mode file alone. A file written before field mode existed carries no marker and loads exactly as it always did.\n\nThe temporal diagnostics stay comparable across the two modes. It and Ito in the metadata are the envelope intensity A(t)^2 in both cases — recovered through the analytic signal on a real grid — so a consumer of a field-mode file sees the same physical quantity as in every envelope file, rather than a carrier-modulated one it would have to demodulate. Luna builds a real-grid pulse as sqrtIcos(omega_0 t) and an envelope-grid pulse as sqrtIexp(iDeltaomega t), so A^2 = I either way.","title":"Reading field-mode output"},{"category":"section","location":"field_mode/#API","page":"Field-Resolved Mode","text":"The field_mode, response and ffac keywords are documented under build_setup on the API Reference page.","title":"API"},{"category":"section","location":"interface/#API-Reference","page":"API Reference","text":"This page lists every exported and documented name in ModelPNPS. The Trace Simulation chapter groups the same entries by role (beam types, window types, setup/scan, primitives, loading) with usage context.","title":"API Reference"},{"category":"section","location":"interface/#Index","page":"API Reference","text":"","title":"Index"},{"category":"section","location":"interface/#Docstrings","page":"API Reference","text":"","title":"Docstrings"},{"category":"module","location":"interface/#ModelPNPS.ModelPNPS","page":"API Reference","text":"High-fidelity forward modelling of PNPS (Parametrized Nonlinear Process Spectrum) pulse-characterisation traces by full spatially-resolved nonlinear propagation, using Luna.jl.\n\nModelPNPS is a standalone package for generating synthetic pulse-characterisation traces directly from the underlying experimental physics — spatial beam overlap, mode shape, mask edges, chromatic vignetting, material dispersion, phase-matching and full χ⁽ⁿ⁾ nonlinear propagation. Given an analytic input pulse and an experimental geometry, it produces the trace a real apparatus would record. These ground-truth traces are intended for testing advanced retrieval algorithms and developing new characterisation techniques.\n\nThe long-term ambition is a complete PNPS trace modeller spanning the full Geib et al. (2019) taxonomy of nonlinear process × parametrization (FROG, d-scan, time-domain ptychography, …). The currently implemented process is TG-FROG (Transient-Grating FROG); see the documentation roadmap for the planned methods.\n\nPhysical model\n\nThe TG-FROG signal in the boxcar geometry is a degenerate four-wave mixing process\n\n    k_signal = k_g2 - k_g1 + k_test\n\nThree input beams (gates g1, g2 and a delayed test pulse t) are crossed inside a thin nonlinear substrate (e.g. UV fused silica). Their interference produces a transient grating; the test pulse diffracts off this grating into the fourth corner of the boxcar. Scanning the test-pulse delay τ and spectrally resolving the diffracted signal yields a 2-D I(ω, τ) spectrogram.\n\nTwo beam models\n\nTwo AbstractInputBeam subtypes are provided:\n\nHE11Beam — the master experimental model. The HE₁₁ mode of a hollow capillary fibre is collimated by a long lens, clipped by a four-hole apodised mask in the collimated beam, then focused into the substrate. Each hole selects one of the four boxcar arms.\nGaussianBeam — a simplified Gaussian-beam model that places three Gaussian beams directly at the correct k-space angles (no mask, no fibre mode). Useful as a sanity-check baseline.\n\nThree signal-extraction window types\n\nThree AbstractSignalWindow subtypes are provided:\n\nPhysicalMaskWindow — the master experimental signal extraction: a frequency-dependent apodised hole in the mask plane (chromatic vignetting is captured exactly).\nPlanckWindow — a smooth, frequency-independent radial Planck taper in k-space (no chromatic vignetting; baseline for the Gaussian model).\nPlanckOmegaWindow — a smooth, frequency-dependent Planck taper that mimics the chromatic vignetting of PhysicalMaskWindow while keeping the smooth-edge advantage. Used to isolate the two effects (smooth-edge vs ω-scaling) within the Gaussian model.\n\nTwo field representations\n\nBy default the propagation is Luna's complex envelope (Grid.EnvGrid): an analytic field about a carrier. build_setup(field_mode=true) instead propagates the real, carrier-resolved field on a Grid.RealGrid, which has no envelope/carrier split, no dropped third-harmonic term and no negative-frequency wrap. That matters when the pulse is only a cycle or two long — at 260 nm a 1 fs pulse is 1.15 optical cycles — where the envelope approximation is marginal by construction and the two representations can be compared directly. It costs roughly twice the memory and three times the time per delay point, so it is a diagnostic, not the production default. See build_setup's field_mode, response and ffac keywords.\n\nHigh-level usage\n\nusing ModelPNPS\nimport Luna.Scans\n\nbeam   = HE11Beam(125e-6, 5.0, 0.1)\nwindow = PhysicalMaskWindow(\n    holex=-0.75e-3, holey=-0.75e-3,\n    holediam=0.5e-3, zmask=0.1)\n\nsetup = build_setup(; λ0=260e-9, τfwhm=2e-15, energy=0.2e-6,\n                      thickness=10e-6, material=:SiO2,\n                      mask_diam=1.0e-3, mask_spacing=0.5e-3,\n                      beam, window)\n\nτ = collect(range(-10e-15, 10e-15, 80))\nexec = Scans.SlurmExec(@__FILE__, length(τ); memory=\"18G\",\n                       arraymode=:batch)\nrun_scan(setup, τ; scan_name=\"my_trace\", exec)\n\nThe full simulation requires SLURM (it is hours of CPU per delay scan with typical grid sizes); the unit tests exercise everything except the actual Luna.run call by passing skip_propagation=true to simulate_delay_point.\n\n\n\n\n\n","title":"ModelPNPS.ModelPNPS"},{"category":"type","location":"interface/#ModelPNPS.AbstractInputBeam","page":"API Reference","text":"AbstractInputBeam\n\nAbstract supertype for the input-beam models from which the three TG-FROG beamlets are constructed. Concrete subtypes: HE11Beam, GaussianBeam.\n\n\n\n\n\n","title":"ModelPNPS.AbstractInputBeam"},{"category":"type","location":"interface/#ModelPNPS.AbstractSignalWindow","page":"API Reference","text":"AbstractSignalWindow\n\nAbstract supertype for k-space windows used to extract the FWM signal beam from the propagated field. Concrete subtypes: PhysicalMaskWindow, PlanckWindow, PlanckOmegaWindow.\n\n\n\n\n\n","title":"ModelPNPS.AbstractSignalWindow"},{"category":"type","location":"interface/#ModelPNPS.ConstantSlice","page":"API Reference","text":"Return the same array for every requested propagation slice.\n\n\n\n\n\n","title":"ModelPNPS.ConstantSlice"},{"category":"type","location":"interface/#ModelPNPS.ExistingSetup","page":"API Reference","text":"Callable wrapper for an already-built setup.\n\n\n\n\n\n","title":"ModelPNPS.ExistingSetup"},{"category":"type","location":"interface/#ModelPNPS.FrozenRamanPolarEnv","page":"API Reference","text":"FrozenRamanPolarEnv(t, r)\n\nEnvelope Raman polarisation response with a frozen response kernel.\n\nWraps Luna.Nonlinear.RamanPolarEnv, precomputing the frequency-domain response function hω once at construction (at unit density). Luna's own response recomputes the time-domain kernel and its FFT on every call, which is negligible for modal simulations (one call per step) but dominant for free-space grids, where the response runs once per transverse point — ~10⁶ calls per RK stage on a 1024² grid, each re-evaluating the 13-mode Hollenbeck–Cantrell sum over the doubled time grid. Freezing is exact here: the density is constant (densityfun = z -> 1) and the :intermediate response ignores its density argument entirely.\n\nThe per-call convolution below reproduces Luna.Nonlinear.(::RamanPolar) line for line, minus the kernel update and with preallocated plan applications; the test suite verifies agreement with Luna's response to machine precision, which guards against drift in Luna's internals.\n\n\n\n\n\n","title":"ModelPNPS.FrozenRamanPolarEnv"},{"category":"type","location":"interface/#ModelPNPS.FusedSignalQuadrantNorm","page":"API Reference","text":"Fused error metric for SignalQuadrantNorm: the DP5 error estimate is computed element-by-element on the fly from the stepper's stage arrays instead of materialising a field-sized yerr array (Luna.RK45 allocates it lazily only for norms without a fused version). Same per-element expression and accumulation order as the materialised path, so the result is bit-identical.\n\n\n\n\n\n","title":"ModelPNPS.FusedSignalQuadrantNorm"},{"category":"type","location":"interface/#ModelPNPS.GaussianBeam","page":"API Reference","text":"GaussianBeam(w0, f_foc)\nGaussianBeam(; w0, f_foc)\n\nA Gaussian beam with 1/e² intensity radius w0 at the focus. f_foc is retained only so the crossing angle (and Δk) can be derived from the same mask geometry as the HE₁₁ model.\n\nFields\n\nw0::Float64     — 1/e² intensity radius at the focus [m]\nf_foc::Float64  — focusing-lens focal length [m] (geometry only)\n\n\n\n\n\n","title":"ModelPNPS.GaussianBeam"},{"category":"type","location":"interface/#ModelPNPS.HE11Beam","page":"API Reference","text":"HE11Beam(a, f_coll, f_foc)\nHE11Beam(; a, f_coll, f_foc)\n\nThe HE₁₁ capillary mode imaged from the fibre output through a collimating lens (f_coll) onto a beam mask, then focused (f_foc) into the substrate. The Hankel transform of the mode has a closed form\n\n    Ẽ(k_⊥) ∝ -a² u₁₁ J₁(u₁₁) J₀(a k_⊥) / (a² k_⊥² - u₁₁²)\n\nwhere u₁₁ is the first zero of J₁. The \"image\" of the fibre core at the substrate has demagnified radius a_scaled = a · f_foc / f_coll.\n\nFields\n\na::Float64         — fibre core radius [m]\nf_coll::Float64    — collimating-lens focal length [m]\nf_foc::Float64     — focusing-lens focal length [m]\n\n\n\n\n\n","title":"ModelPNPS.HE11Beam"},{"category":"type","location":"interface/#ModelPNPS.InputPulseData","page":"API Reference","text":"InputPulseData(ω, Eω)\n\nA measured or simulated input pulse: a complex spectrum Eω on an ABSOLUTE, ascending, (approximately) uniform angular-frequency axis ω [rad/s]. Inject it through build_setup's input_pulse keyword — HE₁₁ beam model only, because there the 1-D reference spectrum IS the pulse and the chromatic mask vignetting is applied to it downstream, so an arbitrary field composes exactly (see build_beamlets). Amplitude units are irrelevant: the beamlet builder rescales the assembled beam to the requested energy.\n\nCompanion utilities, typically chained in this order: load_input_pulse → spectral_window! → center_pulse! → build_setup(...; input_pulse=p).\n\n\n\n\n\n","title":"ModelPNPS.InputPulseData"},{"category":"type","location":"interface/#ModelPNPS.MemorySlice","page":"API Reference","text":"Return a view of a propagation slice held in memory.\n\n\n\n\n\n","title":"ModelPNPS.MemorySlice"},{"category":"type","location":"interface/#ModelPNPS.OutputSlice","page":"API Reference","text":"Read one propagation slice from an output backend.\n\n\n\n\n\n","title":"ModelPNPS.OutputSlice"},{"category":"type","location":"interface/#ModelPNPS.PhysicalMaskWindow","page":"API Reference","text":"PhysicalMaskWindow(holex, holey, holediam, zmask, apod, apod_param)\nPhysicalMaskWindow(; holex, holey, holediam, zmask,\n                   apod = :supergauss, apod_param = nothing)\n\nA frequency-dependent mask hole: physical position (holex, holey) and diameter holediam in the mask plane, sitting zmask (= focal length) upstream of the substrate. The mask plane ↔ k-space mapping is\n\n    (x_mask, y_mask) = (k_x, k_y) · zmask · c / ω\n\nso the same physical hole transmits a wavelength-dependent k-space region (chromatic vignetting). Apodisation choices:\n\n:hard — binary (1 inside the hole, 0 outside).\n:supergauss — exp(-(2 r/d)^n) with n = apod_param (default 16).\n:tanh — smooth 0.5(1 - tanh((r - d/2)/Δ)) with Δ = apod_param in mask-plane metres (default 3 × Δx_mask evaluated at the carrier wavelength).\n\nFields\n\nholex, holey     — hole centre in the mask plane [m]\nholediam         — hole diameter [m]\nzmask            — focal length / mask-to-focus distance [m]\napod             — apodisation type :hard | :supergauss | :tanh\napod_param       — apodisation parameter (nothing → defaults)\n\n\n\n\n\n","title":"ModelPNPS.PhysicalMaskWindow"},{"category":"type","location":"interface/#ModelPNPS.PlanckOmegaWindow","page":"API Reference","text":"PlanckOmegaWindow(xc, yc, holediam, f_foc, pad)\nPlanckOmegaWindow(; xc, yc, holediam, f_foc, pad = 1.25)\n\nA frequency-dependent Planck-taper window. The hole is specified in the mask plane by its centre (xc, yc) and diameter holediam; at frequency ω the window centre and half-width in k-space are\n\n    k_c(ω)    = (ω/c) · (xc, yc) / f_foc\n    k_hole(ω) = (ω/c) · (holediam/2) / f_foc\n\nThis restores the chromatic vignetting of PhysicalMaskWindow while keeping the smooth-edge advantage of PlanckWindow.\n\nFields\n\nxc, yc     — hole centre in the mask plane [m]\nholediam   — hole diameter in the mask plane [m]\nf_foc      — focusing-lens focal length [m]\npad        — outer roll-off multiplier (typically 1.25)\n\n\n\n\n\n","title":"ModelPNPS.PlanckOmegaWindow"},{"category":"type","location":"interface/#ModelPNPS.PlanckWindow","page":"API Reference","text":"PlanckWindow(kxc, kyc, kwidth, pad)\nPlanckWindow(; kxc, kyc, kwidth, pad = 1.25)\n\nA radial Planck-taper window centred at (kxc, kyc) in k-space with flat half-width kwidth and an outer roll-off radius pad·kwidth. The window is frequency-independent: the same mask shape is applied to every spectral component, so chromatic vignetting is removed.\n\nFields\n\nkxc, kyc   — k-space centre of the window [rad/m]\nkwidth     — flat half-width of the window [rad/m]\npad        — multiplier setting the outer roll-off (typically 1.25)\n\n\n\n\n\n","title":"ModelPNPS.PlanckWindow"},{"category":"type","location":"interface/#ModelPNPS.SetupArguments","page":"API Reference","text":"Callable wrapper that resolves and builds setup arguments lazily.\n\n\n\n\n\n","title":"ModelPNPS.SetupArguments"},{"category":"type","location":"interface/#ModelPNPS.SignalQuadrantNorm","page":"API Reference","text":"Callable RK45 error norm built by signal_quadrant_norm.\n\nHolds the signal-quadrant mask, the relative floor, and — on a device — a cached 0/1 indicator of the quadrant on the solver's own array type.\n\n\n\n\n\n","title":"ModelPNPS.SignalQuadrantNorm"},{"category":"type","location":"interface/#ModelPNPS.TGFROGSetup","page":"API Reference","text":"TGFROGSetup\n\nContainer holding everything that is built once (independent of the FROG delay τ): grids, propagation operators, FFT plan, the three pre-built input beamlets, the signal window(s) and the metadata dictionary.\n\nUse build_setup to construct one and simulate_delay_point or run_scan to use it.\n\nFields\n\nThe struct is a passive bundle; fields are not part of the public API and may evolve. Use the constructors and methods provided.\n\n\n\n\n\n","title":"ModelPNPS.TGFROGSetup"},{"category":"type","location":"interface/#ModelPNPS.TraceExtractOutput","page":"API Reference","text":"TraceExtractOutput(setup, zvec, arraytype)\n\nA Luna output handler that reduces each saved z-slice to the trace spectra immediately and keeps only the results, so the full field is never stored, streamed or transferred.\n\nSatisfies the Output interface Luna.run uses (the save call, willsave, metadata calls, and the generic check_cache fallback). Metadata is discarded: ModelPNPS builds its own from setup.combined_grid, and the temp file this replaces was thrown away too.\n\n\n\n\n\n","title":"ModelPNPS.TraceExtractOutput"},{"category":"method","location":"interface/#ModelPNPS._abs_deviation_squared-Tuple{Any, Any}","page":"API Reference","text":"Return the squared deviation of abs(value) from mean_amplitude.\n\n\n\n\n\n","title":"ModelPNPS._abs_deviation_squared"},{"category":"method","location":"interface/#ModelPNPS._beamlet_profile-Tuple{Any, Any, AbstractArray{<:Complex, 3}, Real, Real, Real}","page":"API Reference","text":"_beamlet_profile(grid, xygrid, Eωk, holex, holey, zmask; nr, rmax, nθ=64)\n    -> (r, Eωr, asym)\n\nThe spatially resolved complex focal field of one beamlet, reduced to a radial profile Eωr[ω, r] about its own centre, with the radius axis r in metres and a per-ω measure asym of how well the radial reduction describes it.\n\nDiagnostic only: nothing here feeds the propagation.\n\nWhere the beamlet actually is\n\nIn this representation the beamlets do not sit at BOXCARS corners in real space — they all cross at the focus, and the corners are in k-space. build_he11_kspace builds the field in (ω, ky, kx) with the transverse amplitude the Hankel transform of the HE₁₁ mode, so k-space is the COLLIMATED (mask) plane — makemask maps x = kx·zmask·c/ω — and real space, after ifft over dims 2 and 3, is the FOCAL plane. A hole at mask position (holex, holey) therefore selects k around k₀ = hole·ω/(c·zmask), and in the focal plane that offset is a tilt, not a displacement: measured on the production geometry, the gate beamlet peaks at the real-space grid centre to the pixel, and carries a phase slope of 2.402e5 rad/m against the predicted k₀ = 2.417e5.\n\nSo the centre of this profile is the grid centre. Centring it on the mask-hole position mapped through the focus — 1 mm out, against a 26 µm spot — would sample nothing.\n\nWhy the tilt is removed first\n\nk₀·r reaches 37 rad across the default sampling radius, so an azimuthal average of the raw complex field would annihilate it. The field is demodulated by exp(-i k₀(ω)·r) before sampling, leaving the beamlet's own envelope. k₀ ∝ ω, so the coefficient is a constant hole/(c·zmask) in s/m; it is stored, and multiplying the profile by exp(+iω(cₓx + c_yy)) restores the full field. Note that the removed tilt is physical — a linear delay across the beamlet, i.e. the pulse-front tilt of the crossing geometry — not an artefact.\n\nAccuracy\n\nSampled by bilinear interpolation on nr × nθ polar points (the focal spot is ~27 grid cells across, so the interpolation is not the limiting error) and averaged azimuthally. asym is the azimuthal RMS of |E| over its mean, restricted to radii carrying signal: 1.2–3.8 % on the production geometry, i.e. the radial reduction is a good description but not an exact one, and a consumer can see how good.\n\nIntegrating |Eωr|² with the 2πr dr Jacobian and dividing by the cell area reproduces the stored Iω_beamlet to ~1.5 % at the default nr = 64 — the shortfall is the Airy wings beyond rmax plus that asymmetry, and it converges (0.980/0.987/0.988 at nr = 128 against 0.975/0.984/0.986 at 64, at 200/260/350 nm). The test suite asserts this closure, which is the check that the centre, the Jacobian and the normalisation are all right.\n\n\n\n\n\n","title":"ModelPNPS._beamlet_profile"},{"category":"method","location":"interface/#ModelPNPS._build_setup_resolved-Tuple{NamedTuple}","page":"API Reference","text":"_build_setup_resolved(setup_args) -> TGFROGSetup\n\nBuild the setup, resolving arraytype FIRST and then calling build_setup through Base.invokelatest.\n\nLuna.resolve_arraytype(:cuda) loads the GPU package at run time, and methods defined by a package loaded during a call are not visible to that same call — Julia rejects them as \"too new to be called from this world context\". Resolving first and invoking afterwards puts the construction in a world where the array type's constructors exist.\n\nThis is why a scan script should pass arraytype=:cuda inside setup_args and let this happen on the compute node, rather than loading the GPU package itself.\n\n\n\n\n\n","title":"ModelPNPS._build_setup_resolved"},{"category":"method","location":"interface/#ModelPNPS._completed_scanidcs-Tuple{AbstractString}","page":"API Reference","text":"_completed_scanidcs(scan_name) -> Set{Int}\n\nScan indices already present in <scan_name>_collected.h5, i.e. those whose trace data is not all zero. An empty set if the file does not exist yet.\n\nReads one point at a time: the file may be large and this runs before any propagation.\n\n\n\n\n\n","title":"ModelPNPS._completed_scanidcs"},{"category":"method","location":"interface/#ModelPNPS._empty_extra_outputs-Tuple{Any}","page":"API Reference","text":"Return no additional values for Output.scansave.\n\n\n\n\n\n","title":"ModelPNPS._empty_extra_outputs"},{"category":"method","location":"interface/#ModelPNPS._envelope_intensity-Tuple{Luna.Grid.EnvGrid, Any}","page":"API Reference","text":"_envelope_intensity(grid, Et)\n\n|A(t)|² — the ENVELOPE intensity — from whatever time-domain field grid produces. On an EnvGrid that is Et itself; on a RealGrid the field is carrier-resolved and the envelope is recovered through its analytic signal.\n\nBoth conventions coincide numerically: Luna builds a real-grid pulse as √I·cos(ω₀t) and an envelope-grid pulse as √I·exp(iΔωt), so |A|² = I either way. Keeping the envelope intensity in the output metadata means a consumer of a field-mode file sees the same physical quantity in It/Ito as in every envelope file, rather than a carrier-modulated one it would have to demodulate.\n\n\n\n\n\n","title":"ModelPNPS._envelope_intensity"},{"category":"method","location":"interface/#ModelPNPS._extract_slice_device!-NTuple{6, Any}","page":"API Reference","text":"_extract_slice_device!(Iint, Ireim, Ifull, Ez, wsgn, quadrng)\n\nReduce one (Nω, Nky, Nkx) device slice into the three per-ω spectra, writing into column views of the host result arrays. Three reductions over dims (2, 3) and one small copy back per slice; the field itself never moves.\n\nMathematically identical to extract_signal_spectra plus _quadrant_spectrum!, which is what a slice arriving on the host uses instead. The sums are formed in a different order here, so results agree to rounding rather than bitwise — the standard everywhere else on the device path.\n\nBoth operands of the two windowed reductions have the SAME shape: mapreduce over several arrays does not broadcast (Base throws DimensionMismatch, and a GPU backend may silently compute something else), which is why the re-imaging sign lives inside wsgn rather than in a (1, Nky, Nkx) array of its own. The quadrant sum is a single-array reduction over a strided view, so it reads only the quadrant instead of masking the whole field.\n\n\n\n\n\n","title":"ModelPNPS._extract_slice_device!"},{"category":"method","location":"interface/#ModelPNPS._field_responses-Tuple{Symbol, Any}","page":"API Reference","text":"_field_responses(response, χ3) -> Tuple\n\nThe nonlinear response for a field-resolved (RealGrid) run.\n\n:nothg (and :auto) — (3/4) ε₀ χ³ |E_a|² E via Luna.Nonlinear.Kerr_field_nothg. This is the SAME physics content as the envelope Kerr_env, evaluated on a carrier-resolved field, so an envelope-versus-field comparison made with it isolates representation error with nothing else changed. It is the default for exactly that reason.\n:thg — ε₀ χ³ E³ via Luna.Nonlinear.Kerr_field, which adds the third-harmonic and counter-rotating terms the envelope drops. The difference between the two runs is precisely what the envelope omits.\n\nnote: What `:thg` does and does not propagate on a UV window\nThe third harmonic is generated on the fine grid and then discarded by the crop back to the propagated grid whenever it falls outside the window (at λlims = (143, 600) nm the 3ω band of a 2 fs 260 nm pulse starts above ωmax). The within-band counter-rotating terms are retained, and those are the real difference from the envelope. Propagating the third harmonic itself needs λlims extended to ~λ0/3.\n\n\n\n\n\n","title":"ModelPNPS._field_responses"},{"category":"method","location":"interface/#ModelPNPS._hdf5_dataset-Tuple{Any, Any}","page":"API Reference","text":"Return the HDF5 dataset stored at key, or throw for a malformed file.\n\n\n\n\n\n","title":"ModelPNPS._hdf5_dataset"},{"category":"method","location":"interface/#ModelPNPS._hdf5_group-Tuple{Any, Any}","page":"API Reference","text":"Return the HDF5 group stored at key, or throw for a malformed file.\n\n\n\n\n\n","title":"ModelPNPS._hdf5_group"},{"category":"method","location":"interface/#ModelPNPS._is_field_mode-Tuple{Luna.Grid.RealGrid}","page":"API Reference","text":"Whether grid is field-resolved (real) rather than an envelope grid.\n\n\n\n\n\n","title":"ModelPNPS._is_field_mode"},{"category":"method","location":"interface/#ModelPNPS._is_scan_dataset-Tuple{Any}","page":"API Reference","text":"Return whether an HDF5 key names a scan result dataset.\n\n\n\n\n\n","title":"ModelPNPS._is_scan_dataset"},{"category":"method","location":"interface/#ModelPNPS._norm_name-Tuple{ModelPNPS.SignalQuadrantNorm}","page":"API Reference","text":"Provenance label for the error norm used by a scan.\n\n\n\n\n\n","title":"ModelPNPS._norm_name"},{"category":"method","location":"interface/#ModelPNPS._plan_1d-Tuple{Luna.Grid.EnvGrid}","page":"API Reference","text":"_plan_1d(grid)\n\nForward transform plan for the 1-D reference pulse: complex for an EnvGrid, real-to-complex for a RealGrid. Fields.GaussField dispatches its time-domain shape on the grid type but takes the plan from the caller, so the two have to be chosen together.\n\n\n\n\n\n","title":"ModelPNPS._plan_1d"},{"category":"method","location":"interface/#ModelPNPS._profile_meta-NTuple{10, Any}","page":"API Reference","text":"_profile_meta(r, Eωr, asym, coef, rmax_req, holex, holey, zmask,\n              rmax_units, which) -> Dict\n\nPackage _beamlet_profile's output for the output file. Complex data is split into two real datasets, matching the Eω_beamlet_re/_im convention (h5py reads HDF5.jl's native complex compound awkwardly), and enough geometry is recorded for the file to be self-describing without the script that made it.\n\n\n\n\n\n","title":"ModelPNPS._profile_meta"},{"category":"method","location":"interface/#ModelPNPS._quadrant_spectrum!-Tuple{Any, AbstractArray{<:Complex, 3}, AbstractMatrix}","page":"API Reference","text":"_quadrant_spectrum!(out, Ez, quad)\n\nAccumulate |E|² over the transverse points selected by the (Nky, Nkx) mask quad into the length-Nω vector out, without materialising any field-sized temporary (the previous broadcast allocated two per z-slice).\n\n\n\n\n\n","title":"ModelPNPS._quadrant_spectrum!"},{"category":"method","location":"interface/#ModelPNPS._read_optional_dataset-Tuple{Any, Any}","page":"API Reference","text":"Read key when it exists, otherwise return nothing.\n\n\n\n\n\n","title":"ModelPNPS._read_optional_dataset"},{"category":"method","location":"interface/#ModelPNPS._reduce_slice!-Tuple{ModelPNPS.TraceExtractOutput, Any, Any}","page":"API Reference","text":"_reduce_slice!(o::TraceExtractOutput, Ez, iz)\n\nReduce one saved slice into column iz, routing on where the slice actually is.\n\nA device propagation delivers every slice on the device — including z = 0, which is the step start rather than an endpoint and so comes through the interpolant, because Luna.needs_host_save declines the copy HostOutput would otherwise make.\n\nThe host branch therefore serves a host propagation (extract_on_save=true on the CPU), and any save that is genuinely interpolated. It goes through the original extract_signal_spectra/_quadrant_spectrum! kernels against the plain host window the setup already holds: no extra memory, no transfer, and the host result is bit-identical to the save-the-stack route by construction rather than by a parallel implementation that could drift.\n\n\n\n\n\n","title":"ModelPNPS._reduce_slice!"},{"category":"method","location":"interface/#ModelPNPS._resolve_zsave-Tuple{Integer, Real}","page":"API Reference","text":"_resolve_zsave(zsave, zmax) -> Vector{Float64}\n\nResolve the zsave propagation-snapshot specification into a validated, sorted vector of z positions [m] at which the field is saved during propagation.\n\nzsave::Integer — a uniform grid of zsave points over [0, zmax] (range(0, zmax, zsave)), reproducing the legacy nz behaviour exactly (including the entrance slice at z=0 and the exit slice at z=zmax).\nzsave::AbstractVector — explicit material thicknesses [m]. Must be strictly increasing, all >= 0, and all <= zmax. zmax is appended if not already present (within rtol=1e-12) so the full-thickness (\":end\") slice always exists.\n\nBecause the propagation is a forward-marching integrator with z-independent dynamics, the field saved at an intermediate z is identical to a dedicated run of thickness z, so a single zmax run yields every shorter thickness for free.\n\nThe function is idempotent: re-resolving an already-resolved vector (which the integer path produces with an entrance slice at z=0) returns it unchanged, so it is safe to call more than once on the same grid.\n\n\n\n\n\n","title":"ModelPNPS._resolve_zsave"},{"category":"method","location":"interface/#ModelPNPS._response_name-Tuple{Bool, Symbol, Bool}","page":"API Reference","text":"Canonical name of the response actually used, for the output metadata.\n\n\n\n\n\n","title":"ModelPNPS._response_name"},{"category":"method","location":"interface/#ModelPNPS._scan_peak-Tuple{Any}","page":"API Reference","text":"_scan_peak(dset) -> Float64\n\nLargest absolute value over every computed delay point of a collected trace dataset. Read one point at a time rather than whole: this runs against a file a scan may still be writing, and the datasets grow with the delay count.\n\n\n\n\n\n","title":"ModelPNPS._scan_peak"},{"category":"method","location":"interface/#ModelPNPS._shift_spectrum-Tuple{Any, Any, Bool}","page":"API Reference","text":"Shift selected envelope-spectrum dimensions into natural order.\n\n\n\n\n\n","title":"ModelPNPS._shift_spectrum"},{"category":"method","location":"interface/#ModelPNPS._shift_spectrum-Tuple{Any, Bool}","page":"API Reference","text":"Shift an envelope spectrum into natural order; field spectra are already ordered.\n\n\n\n\n\n","title":"ModelPNPS._shift_spectrum"},{"category":"method","location":"interface/#ModelPNPS._signed_window-Tuple{AbstractArray{<:Real}, Any, Integer}","page":"API Reference","text":"_signed_window(w, arraytype, Nω) -> array\n\nThe signal window with the re-imaging sign pattern (-1)^((iky-1)+(ikx-1)) folded in, on arraytype.\n\nFolding the sign into the window lets one array serve both reductions of extract_signal_spectra: the signed sum needs it, and the intensity sum is unaffected because |±w·E|² == |w·E|². Carrying a separate sign array instead would cost a second field-sized device array, and could not be broadcast into the reduction anyway (see the shape note in _sqn_fused).\n\nA 2-D window is expanded to 3-D: the reduction takes mapreduce over two arrays, which does not broadcast shapes.\n\n\n\n\n\n","title":"ModelPNPS._signed_window"},{"category":"method","location":"interface/#ModelPNPS._sqn_devmask!-Tuple{ModelPNPS.SignalQuadrantNorm, Any}","page":"API Reference","text":"The quadrant indicator as a (1, Nky, Nkx) array on y's array type, built once and cached on the norm. Broadcasting it into the reduction costs one small array (8 MB even at the largest campaign shape) and keeps the reduction over whole, contiguous arrays.\n\nThe mask is built from the same BitMatrix the host path uses, via quadrant_ranges — so its rectangle assertion still guards the device path.\n\n\n\n\n\n","title":"ModelPNPS._sqn_devmask!"},{"category":"method","location":"interface/#ModelPNPS._sqn_fused-Tuple{Luna.Utils.DeviceBackend, ModelPNPS.SignalQuadrantNorm, Any}","page":"API Reference","text":"Device version of _sqn_fused: the same six sums, computed as reductions along ω into one partial per transverse point, which are then split by quadrant.\n\nEvery operand of a reduction here has the SAME shape. That is deliberate: mapreduce over several arrays does not broadcast shapes (Base throws DimensionMismatch, and a GPU backend may quietly compute something else instead), so the (1, Nky, Nkx) quadrant mask cannot be folded into the reduction and is applied afterwards, to the small per-transverse-point partials. Reducing over strided views of the quadrant would also work in principle, but whole-array reductions are the shape the rest of Luna's device code uses and the one best supported across backends.\n\nBoth halves are summed directly rather than one being total - other, so no cancellation is involved. The error estimate is never materialised — it is formed inside the reduction kernel.\n\n\n\n\n\n","title":"ModelPNPS._sqn_fused"},{"category":"method","location":"interface/#ModelPNPS._to_time-Tuple{Luna.Grid.EnvGrid, AbstractVector}","page":"API Reference","text":"_to_time(grid, Eω)\n\nThe 1-D time-domain field for a spectrum on grid's own frequency axis: an inverse FFT for an EnvGrid (whose spectrum is FFT-ordered about the carrier) and an inverse real FFT for a RealGrid (whose spectrum is the monotonic rfft half-spectrum of a real field).\n\n\n\n\n\n","title":"ModelPNPS._to_time"},{"category":"method","location":"interface/#ModelPNPS._trace_results-Tuple{ModelPNPS.TGFROGSetup, ModelPNPS.TraceExtractOutput}","page":"API Reference","text":"_trace_results(setup, o::TraceExtractOutput) -> NamedTuple\n\nThe same NamedTuple the save-the-stack route returns, assembled from an extraction handler. Kept next to that route's assembly block so the two cannot drift.\n\n\n\n\n\n","title":"ModelPNPS._trace_results"},{"category":"method","location":"interface/#ModelPNPS.a_scaled-Tuple{ModelPNPS.HE11Beam}","page":"API Reference","text":"a_scaled = fibre core radius imaged onto the focal plane.\n\n\n\n\n\n","title":"ModelPNPS.a_scaled"},{"category":"method","location":"interface/#ModelPNPS.apply_delay-Tuple{AbstractArray{<:Complex, 3}, Luna.Grid.TimeGrid, Real}","page":"API Reference","text":"apply_delay(Eωk, grid, τ) -> Array{ComplexF64,3}\n\nApply a time delay τ (seconds) to a frequency-domain field by multiplying each spectral component by exp(-i ω τ). τ = 0 returns a copy equal to the input.\n\n\n\n\n\n","title":"ModelPNPS.apply_delay"},{"category":"method","location":"interface/#ModelPNPS.apply_tilt-Tuple{AbstractArray{<:Complex, 3}, Luna.Grid.FreeGrid, Real, Real}","page":"API Reference","text":"apply_tilt(Eωxy, xygrid, Δkx, Δky) -> Array{ComplexF64,3}\n\nMultiply a real-space field E(ω, y, x) by the phase ramp exp(i Δkx · x) · exp(i Δky · y), which shifts its centre by (Δky, Δkx) in k-space (after FFT). Δkx = Δky = 0 is the identity.\n\n\n\n\n\n","title":"ModelPNPS.apply_tilt"},{"category":"method","location":"interface/#ModelPNPS.build_beamlets-Tuple{ModelPNPS.HE11Beam, Luna.Grid.TimeGrid, Luna.Grid.FreeGrid, Any, AbstractVector, Real, Any}","page":"API Reference","text":"build_beamlets(beam, grid, xygrid, geom, Eω, energy, energyfun_ω;\n               apod=:supergauss, apod_param=nothing, ϕ=nothing,\n               profile=true, profile_nr=64, profile_rmax_units=6)\n    -> (Eωk_g1, Eωk_g2, Eωk_t_base, Iω_beamlet, beam_metadata::Dict)\n\nConstruct the input beamlets at the substrate, in k-space. The geometry geom is a NamedTuple(mask_diam, mask_spacing, f_foc, λ0, τfwhm, geometry) shared by both beam models. geom.geometry is :tg for the three-beam boxcar layout (g1, g2, t-base), or :sd for the two-beam self-diffraction layout — HE11Beam only, and rejected for any other beam by build_setup — which returns nothing in place of Eωk_g2 (a zero array of that size is half a gigabyte of pure waste) and puts the probe in Eωk_g1 and the delayed gate in Eωk_t_base.\n\nϕ is accepted for a uniform interface across beam models and ignored by both: the spectral phase is already carried by the 1-D reference Eω that build_setup passes in. profile, profile_nr and profile_rmax_units control the diagnostic radial focal profile added to beam_metadata; see _beamlet_profile.\n\nFor HE11Beam: builds the full HE₁₁ k-space field, rescales to the requested energy, then applies three apodised hole masks (g1, g2, t). Each beamlet sits at one of the boxcar corners. Iω_beamlet is the spatially-integrated spectrum of g1 (used as a chromatic-vignetting diagnostic in the output file).\n\nFor GaussianBeam: builds a Gaussian-Gaussian field with energy energy/3 per beam, ifft's to real space, then applies real-space tilts to position the three beams at the boxcar corners. Iω_beamlet here is just the (unvignetted) input spectrum scaled to energy/3; it is returned for uniformity with the HE₁₁ model so downstream code never special-cases the beam type.\n\n\n\n\n\n","title":"ModelPNPS.build_beamlets"},{"category":"method","location":"interface/#ModelPNPS.build_gaussian_kspace-Tuple{Luna.Grid.TimeGrid, Luna.Grid.FreeGrid, ModelPNPS.GaussianBeam, Any, Any, Any}","page":"API Reference","text":"build_gaussian_kspace(grid, xygrid, beam::GaussianBeam,\n                      λ0, τfwhm, energy) -> Array{ComplexF64,3}\n\nConstruct the 3-D field E(ω, ky, kx) for a Gaussian-Gaussian spatio-temporal pulse: temporal Gaussian envelope (FWHM = τfwhm) at carrier λ0, spatial Gaussian (1/e² radius = beam.w0) centred on the grid, with total spectral energy normalised to energy. Internally uses Luna.Fields.GaussGaussField and Luna.setup (with no nonlinearity) to construct the field, then discards the throw-away transform/FT.\n\n\n\n\n\n","title":"ModelPNPS.build_gaussian_kspace"},{"category":"method","location":"interface/#ModelPNPS.build_he11_kspace-Tuple{Luna.Grid.TimeGrid, Luna.Grid.FreeGrid, ModelPNPS.HE11Beam, AbstractVector}","page":"API Reference","text":"build_he11_kspace(grid, xygrid, beam::HE11Beam, Eω) -> Array{ComplexF64,3}\n\nConstruct the 3-D field E(ω, ky, kx) for the HE₁₁ capillary mode imaged onto the focal plane, multiplied by the 1-D spectral pulse Eω. Phase ramps shift the beam from the FFTW corner to the centre of the spatial grid.\n\nThe closed-form Hankel transform of the J₀ mode profile is used; the a²k² - u₁₁² denominator is finite at all (kx, ky) sample points for reasonable grid sizes (the singular ring is at radius u₁₁/a, well outside typical Nyquist limits at the focal-plane scale).\n\n\n\n\n\n","title":"ModelPNPS.build_he11_kspace"},{"category":"method","location":"interface/#ModelPNPS.build_setup-Tuple{}","page":"API Reference","text":"build_setup(; λ0, τfwhm, energy, thickness, material,\n              mask_diam, mask_spacing, beam, window,\n              kwargs...) -> TGFROGSetup\n\nBuild the once-per-simulation setup: temporal/spatial grids, propagation operators, FFT plans, the three input beamlets and the signal window(s). The defaults reproduce the master script context/tgfrog_DUV_mask_apod6.jl.\n\nRequired keyword arguments\n\nλ0, τfwhm, energy         — pulse carrier wavelength [m], intensity                                   FWHM [s], total pulse energy [J]\nthickness, material         — substrate thickness [m] and Luna                                   PhysData material symbol (e.g. :SiO2)\nmask_diam, mask_spacing     — mask hole diameter [m] and edge-to-edge                                   gap [m]\nbeam::AbstractInputBeam       — input-beam model                                   (HE11Beam or                                   GaussianBeam)\nwindow                        — signal-extraction window: a single                                   AbstractSignalWindow or a                                   vector of them (the latter is used by the                                   Gaussian example to save both the                                   ω-independent and ω-dependent windows in                                   one run)\n\nOptional keyword arguments\n\ntrange = 40e-15               — temporal window [s]\nλlims  = (160e-9, 500e-9)     — wavelength window [m]\nR, N                          — spatial half-width [m] and grid size; if                                   either is nothing, both are computed                                   via optimal_spatial_grid\napod, apod_param              — apodisation for the input-beamlet                                   masks (only relevant for HE11Beam)\ngeometry = :tg                — beam layout. :tg is the four-hole boxcar                                   TG-FROG geometry (three inputs, signal in the                                   fourth corner); :sd places two collinear                                   holes for self-diffraction, whose 2k_E - k_G                                   signal sits one slot further out on the same                                   axis. It selects both the beamlet layout and                                   the k-space bound used by                                   optimal_spatial_grid. :sd is                                   implemented for HE11Beam only; any other                                   value throws an ArgumentError\nfftsize = :pow2               — how the temporal sample count is rounded up:                                   :pow2 to the next power of two, :smooth to                                   the next even 2,3,5-smooth size (a smaller grid                                   for the same resolution). Envelope mode only —                                   Grid.RealGrid has no such control\nGDD = 0.0, TOD = 0.0        — group-delay and third-order dispersion                                   [s², s³] applied to the input pulse\ninput_pulse = nothing         — an InputPulseData: use this                                   measured/simulated complex spectrum as the                                   source instead of the analytic Gaussian                                   (HE11Beam only). λ0/τfwhm then serve                                   only as nominal values (mask apodisation                                   defaults, diagnostics, metadata); energy                                   still sets the beam energy (the data's                                   amplitude scale is irrelevant); GDD/TOD                                   compose on top if nonzero. See                                   load_input_pulse,                                   spectral_window!,                                   center_pulse!\nraman = false                 — include the delayed (Raman) part of the                                   nonlinear response via                                   FrozenRamanPolarEnv; requires a                                   material with an :intermediate Raman                                   model in Luna.PhysData.raman_parameters                                   (for :SiO2 the multimode                                   Hollenbeck–Cantrell response). The total                                   polarisation is                                   (3/4)ε₀χ³[(1-f_R)|E|²E + f_R E(h_R⊛|E|²)]                                   — equal prefactors on both terms, the                                   envelope-defined f_R convention of                                   Luna's prop_gnlse, so the quasi-static                                   limit reproduces the Kerr-only response                                   exactly\nraman_fraction = 0.18         — envelope-defined nuclear fraction f_R                                   of χ³ (the Blow–Wood silica value)\nraman_impl = :batched         — Raman implementation: :batched computes                                   the convolution for all transverse points                                   at once (two batched FFTs per RHS                                   evaluation); :frozen is the legacy                                   per-column FrozenRamanPolarEnv.                                   Results agree to rounding accuracy\nfield_mode = false            — propagate the real, carrier-resolved field on a                                   Luna.Grid.RealGrid instead of the complex envelope on                                   an EnvGrid. There is then no carrier/envelope split,                                   no dropped third-harmonic term and no negative-frequency                                   wrap; the cost is roughly 2× the memory and 3×                                   the time per delay point (measured 3.0× at N = 64 and                                   3.3× at N = 128, at matched step counts). The envelope                                   path is untouched and remains the default\nresponse = :auto              — field-mode nonlinearity: :nothg (= :auto)                                   for (3/4) ε₀ χ³ |E_a|² E, the same physics                                   content as the envelope Kerr_env and hence the                                   response for an envelope-versus-field comparison;                                   :thg for ε₀ χ³ E³,                                   which adds what the envelope drops. Ignored unless                                   field_mode = true\nffac = 6                      — field-mode nonlinear-grid sampling factor,                                   forwarded to                                   Grid.RealGrid. 6 (the default) sizes the fine grid for                                   E³; 4 is enough for :nothg alone and typically                                   removes the oversampling entirely, halving memory and                                   per-step cost. It changes the grid, so use it only with                                   a convergence check against the default\nraman                         — not implemented in field mode (see the error message                                   there for why)\nbeamlet_profile = true        — store the gate beamlet's spatially resolved complex                                   focal field as a radial profile Eω_beamlet_r_re/_im                                   (Nω, nr) plus the radius axis beamlet_r in metres,                                   so the pulse that actually drives the signal can be                                   computed rather than assumed. Diagnostic only — no                                   propagation result depends on it — costing one 2-D                                   inverse transform per ω ONCE at setup and ~130 kB in                                   the file. See _beamlet_profile for where the                                   beamlet is (the focus, not a BOXCARS corner) and why                                   the geometric tilt is removed first\nbeamlet_profile_nr = 64       — radial samples. Measured radial closure against                                   Iω_beamlet on the production geometry, at 200 /                                   260 / 350 nm: 0.955/0.972/0.980 at nr = 32,                                   0.975/0.984/0.986 at 64, 0.980/0.987/0.988 at 128.                                   64 is where it has essentially converged, for 262 kB                                   at Nω = 256; the residual ~1.5 % is truncation at                                   rmax plus the beamlet's real azimuthal asymmetry\nbeamlet_profile_rmax_units = 6 — outer radius, in units of λ0·f_foc/mask_diam                                   (w0 for GaussianBeam)\nfactored_linop = true         — use Luna's lazy (factored) linear operator                                   and normalisation, saving two field-sized                                   arrays; bit-identical to the materialised                                   versions\nstore_window = true           — store the materialised window array(s) in                                   the output metadata (≈1 GiB at production                                   size); the window parameters (window_def)                                   are always stored and reconstruct the                                   array via build_window\narraytype = Array             — array type the propagation runs on. Pass                                   :cuda to build the beamlets, operators and                                   window on the GPU; it is resolved lazily, so a                                   scan script passes it inside setup_args                                   rather than as a run_scan keyword and                                   the GPU package is then loaded on the compute                                   node, never on the submitting host\nbeamlets_on_host = false      — on a device run, keep the pre-built beamlets in                                   host memory and upload the delayed sum once per                                   delay point — two fewer resident device fields                                   in exchange for one transfer per point. Use it                                   when the card is memory-bound; see                                   memory_budget\noptimal_grid_kwargs           — extra kwargs forwarded to                                   optimal_spatial_grid\nextra_grid_metadata           — additional entries merged into the                                   output combined_grid dict\n\n\n\n\n\n","title":"ModelPNPS.build_setup"},{"category":"method","location":"interface/#ModelPNPS.build_window-Tuple{ModelPNPS.PhysicalMaskWindow, Luna.Grid.TimeGrid, Luna.Grid.FreeGrid}","page":"API Reference","text":"build_window(w::AbstractSignalWindow, grid, xygrid; λ0=nothing)\n    -> Array{Float64, N}\n\nMaterialise the precomputed signal-extraction window. Returns a (Nky, Nkx) 2-D array for PlanckWindow and a (Nω, Nky, Nkx) 3-D array for PhysicalMaskWindow and PlanckOmegaWindow. λ0 is forwarded to makemask for default :tanh apodisation widths only.\n\n\n\n\n\n","title":"ModelPNPS.build_window"},{"category":"method","location":"interface/#ModelPNPS.center_pulse!-Tuple{ModelPNPS.InputPulseData}","page":"API Reference","text":"center_pulse!(p::InputPulseData; oversample=8) -> (p, tshift)\n\nRemove the linear spectral-phase component so the temporal intensity envelope peaks at the data FFT's natural origin (array index 1), returning the applied shift tshift [s] (positive = the pulse arrived late and was advanced). A pure linear phase is physically irrelevant; numerically, centring minimises the trange the simulation needs to hold the pulse plus the delay scan, and — more importantly — it is what makes the spectrum interpolatable: a pulse far from its grid's natural time origin has a spectral phase rotating by up to π per sample, which no Re/Im interpolation can resample (interp_input_pulse warns if it sees this). interp_input_pulse then re-anchors the interpolated field at t = 0, the middle sample of Luna's centred target time grid. Requires an (approximately) uniform ω grid. The returned shift is reported modulo the data grid's time period (the on-grid phase is identical for any branch).\n\n\n\n\n\n","title":"ModelPNPS.center_pulse!"},{"category":"method","location":"interface/#ModelPNPS.delayed_input-Tuple{ModelPNPS.TGFROGSetup, Real}","page":"API Reference","text":"delayed_input(setup, τ) -> Array{ComplexF64,3}\n\nCoherent input field for scan delay τ, in the gate-delay convention: the stored trace T(ω τ) has the GATE pair delayed by +τ relative to the probe. Physically the probe arm carries the delay stage, so the probe is delayed by -τ, which equals gating at +τ up to a global time shift that the time-integrating measurement cannot see. The same-τ gate pair stays untouched, so the geometrical smearing structure of the crossed-beam layout (a same-τ gate pair) is preserved. Files written with this convention carry /grid/delay_convention = \"gate\" and need NO delay-axis reversal on loading (retrieval loaders can detect the marker; legacy marker-less files are reversed as before).\n\n\n\n\n\n","title":"ModelPNPS.delayed_input"},{"category":"method","location":"interface/#ModelPNPS.extract_signal_spectra-Tuple{AbstractArray{<:Complex, 3}, AbstractArray{<:Real}, Luna.Grid.FreeGrid}","page":"API Reference","text":"extract_signal_spectra(Eωk, window_array, xygrid)\n    -> (Iω_integrated, Iω_reimaged)\n\nApply a precomputed signal window to a propagated field and extract two spectral diagnostics:\n\nIω_integrated — |E|² summed over all (ky, kx). Models a spectrometer collecting all the signal light.\nIω_reimaged   — |E|² at the centre pixel of the IFFT'd field. Models a spectrometer fed only by the on-axis re-collimated signal.\n\nEωk is either a single (Nω, Nky, Nkx) slice, for which both spectra are length-Nω vectors, or the (Nω, Nky, Nkx, Nz) stack Luna.run produces, for which both are (Nω, Nz). The 4-D method loops over z and calls the 3-D one, so its peak extra memory is one windowed slice rather than a second copy of the whole stack (tens of GB at production size).\n\nwindow_array is broadcast over ω (if 2-D) or matched directly (if 3-D), and over the Nz z-slices in either case.\n\n\n\n\n\n","title":"ModelPNPS.extract_signal_spectra"},{"category":"method","location":"interface/#ModelPNPS.interp_input_pulse-Tuple{Luna.Grid.TimeGrid, ModelPNPS.InputPulseData}","page":"API Reference","text":"interp_input_pulse(grid, p::InputPulseData) -> Vector{ComplexF64}\n\nThe pulse's complex spectrum on grid.ω (the grid's ABSOLUTE frequency axis), zero outside the data's range. Real and imaginary parts are interpolated separately with cubic B-splines, which is accurate when the data grid is finer than the simulation grid — a warning is emitted if it is not (then spectral detail is being invented between samples; supply denser data instead). The input is expected to have been moved to its data FFT's natural origin with center_pulse!. After interpolation, the field is shifted to the middle sample of Luna's centred target time grid, matching Luna's native Fields.DataField convention.\n\n\n\n\n\n","title":"ModelPNPS.interp_input_pulse"},{"category":"method","location":"interface/#ModelPNPS.load_input_pulse-Tuple{AbstractString}","page":"API Reference","text":"load_input_pulse(path; ω_key=\"ω\", Eω_key=\"Eω\") -> InputPulseData\n\nRead an InputPulseData from an HDF5 file: an absolute angular frequency axis under ω_key and a complex spectrum under Eω_key (a native complex dataset, e.g. as written by HDF5.jl or h5py).\n\n\n\n\n\n","title":"ModelPNPS.load_input_pulse"},{"category":"method","location":"interface/#ModelPNPS.load_simulated_scan-Tuple{AbstractString}","page":"API Reference","text":"load_simulated_scan(filename; window_key=\"Iω_win\", z_index=:end,\n                    z_thickness=nothing) -> NamedTuple\n\nRead the raw HDF5 file produced by run_scan and return its contents as a NamedTuple, with all ω-dependent arrays fftshifted into natural (centred) order and the requested z slice(s) extracted from the propagated trace.\n\nArguments\n\nfilename: path to the <scan_name>_collected.h5 file.\n\nKeyword arguments\n\nwindow_key=\"Iω_win\": which scansave dataset to use as the FROG trace. Common choices:\n\"Iω_win\" — full-beam k-space integrated spectrum\n\"Iω_win_reimaged\" — on-axis re-imaged spectrum\n\"Iω_win_ωdep\" — ω-dependent window (Gaussian two-window setup)\n\"Iω_win_ωdep_reimaged\" — ω-dependent re-imaged\nz_index=:end: which propagation z slice to use; the default :end picks the final (full-propagation) slice. Pass an Int for a specific slice index, or :all to return every z slice as a (Nω, nz, Nτ) stack (the equivalent of the trace at every saved material thickness).\nz_thickness=nothing: select the slice whose saved z position [m] is nearest this material thickness. Requires /grid/zsave in the file (written by recent run_scan runs); takes precedence over z_index.\n\nReturned NamedTuple\n\nfield shape description\nω (Nω,) absolute angular frequency [rad/s], natural order\nω0 scalar carrier angular frequency [rad/s] (from /grid/ω0)\nt (Nt,) time grid [s]\nτ (Nτ,) scan-variable delay grid [s]\ntrace 2-D or 3-D FROG trace; natural ω order; 3-D for :all\nzsave (nz,) realized propagation z positions [m]\nIω (Nω,) reference pulse spectrum, natural ω order\nIt (Nt,) reference pulse temporal intensity\nτfwhm scalar input pulse FWHM [s]\nIω_beamlet (Nω,) input-vignetted beamlet spectrum\nIt_beamlet (Nt,) beamlet temporal intensity\nIto_beamlet (Nto,) 8× oversampled beamlet intensity; shares To\nTo (Nto,) 8× oversampled time grid [s]\nIto (Nto,) 8× oversampled temporal intensity\n\nThe optional zsave, It_beamlet, Ito_beamlet, To, and Ito fields are returned only when their corresponding datasets are present.\n\nTo inspect the full signal-beam collection (and hence the exact collection / chromatic-vignetting efficiency Iω_win ./ Iω_full), load the signal-quadrant reference with window_key=\"Iω_full\".\n\n\n\n\n\n","title":"ModelPNPS.load_simulated_scan"},{"category":"method","location":"interface/#ModelPNPS.makemask-Tuple{Real, Real, Real, Luna.Grid.TimeGrid, Luna.Grid.FreeGrid}","page":"API Reference","text":"makemask(holex, holey, holediam, grid, xygrid;\n         zmask, apod=:supergauss, apod_param=nothing,\n         λ0_for_default=nothing) -> Array{Float64,3}\n\nBuild a 3-D (Nω, Nky, Nkx) apodised-hole mask. For each (ω, ky, kx) sample, the k-vector is mapped to the mask-plane position x = kx · zmask · c / ω (and likewise for y), and a hole of diameter holediam centred at (holex, holey) is evaluated.\n\nλ0_for_default is only used when apod=:tanh and apod_param===nothing, in which case the smoothing width is set to 3·Δx_mask evaluated at the carrier wavelength.\n\n\n\n\n\n","title":"ModelPNPS.makemask"},{"category":"method","location":"interface/#ModelPNPS.memory_budget-Tuple{NamedTuple}","page":"API Reference","text":"memory_budget(setup_args::NamedTuple) -> NamedTuple\n\nResident device memory one delay point of setup_args will need, and the host peak build_setup will reach, broken down by buffer. setup_args is the same NamedTuple run_scan and verify_against_collected take; only the grid-determining entries are read, and building the 1-D time grid is the whole cost, so this is free to call.\n\nThis exists because guessing is expensive. The envelope path obeys a simple rule — 9 RK45 registers plus one transform buffer, i.e. 10× the field size, measured exactly on an A40 — and the field path does not: its state is twice as long in ω, its nonlinear evaluation runs on a grid twice as long again in time, and the no-THG response carries a complex analytic-signal buffer on that grid. At the 40 µm production shape (N = 768) that is 92 GiB against the envelope's 24. Finding this out by running is an hour of rented GPU and a dead process.\n\nwarning: The response's buffer appears on the first RHS, not at setup\nNonlinear.KerrFieldNoTHG allocates its analytic-signal buffer lazily, when it first sees a field. A card with room to spare after build_setup can therefore still die on the first step — 18 GiB later at the production shape. This function counts it; a measurement taken after build_setup alone will not.\n\nFields: Nω, Nt, Nto, Nωo, field (one state array), the per-buffer terms state, et_win, eto, ewo, pto, analytic, window, input, and the totals device and host. All in GiB.\n\nstate … window are what the transform and solver hold; input is the per-delay field delayed_input produces. Only the first group is allocated by build_setup, so a measurement taken across build_setup alone will fall short of device by state, analytic and input — those appear when the first delay point runs.\n\nThe buffer set and its aliasing are NonlinearRHS.TransFree's: Pωo always aliases Eωo (the inverse transform consumes it), Pto aliases Eto when every response is pointwise (the envelope Kerr, and field :thg, but not field :nothg), and Et_win exists only when the grid is oversampled. window is the extraction window, which is device-resident when save-time extraction is used — the default on a device.\n\n\n\n\n\n","title":"ModelPNPS.memory_budget"},{"category":"method","location":"interface/#ModelPNPS.optimal_spatial_grid-NTuple{5, Any}","page":"API Reference","text":"optimal_spatial_grid(f, mask_diam, mask_spacing, λmin, λmax;\n                     n_airy=5, pts_per_lobe=10, safety=1.5,\n                     margin=1.1, geometry=:tg) -> (R, N)\n\nReturn (R, N) for a Luna FreeGrid(R, N) chosen so that the spatial grid\n\ncontains at least n_airy Airy diffraction patterns of the longest wavelength λmax from a mask hole of diameter mask_diam focused by a lens of focal length f (real-space containment), and\nresolves the Airy pattern at the shortest wavelength λmin with at least pts_per_lobe points across the central lobe (real-space resolution), and\nhas a k-space half-extent that comfortably encloses the FWM nonlinear k-vectors generated at λmin from the outermost mask hole, with a safety headroom factor (k-space containment).\n\nN is rounded up to the next power of 2 for FFT efficiency. Diagnostic information is printed via @info.\n\nArguments\n\nf: focal length of the focusing lens [m].\nmask_diam: diameter of each mask hole [m].\nmask_spacing: edge-to-edge spacing between adjacent mask holes [m].\nλmin, λmax: shortest and longest wavelengths the simulation must represent [m]. These should bracket the input spectrum and its FWM products.\n\nKeyword arguments\n\nn_airy=5: number of Airy patterns the grid should contain at λmax.\npts_per_lobe=10: real-space samples across the central Airy lobe at λmin.\nsafety=1.5: multiplier on the required nonlinear k-vector envelope to guard against aliasing.\nmargin=1.1: multiplier on the resolved grid size before rounding up to the next even 2,3,5-smooth FFT size (guards the containment against grid quantisation).\ngeometry=:tg: the beam layout the k-space bound (3.) is computed for. :tg is the four-hole boxcar, whose χ⁽³⁾ combinations reach three times the hole offset; :sd is the two-hole self-diffraction layout, whose 2k₁ - k₂ signal sits one further slot out along the same axis. See the comment on x_max in the implementation for the two bounds.\n\n\n\n\n\n","title":"ModelPNPS.optimal_spatial_grid"},{"category":"method","location":"interface/#ModelPNPS.quadrant_ranges-Tuple{AbstractMatrix{Bool}}","page":"API Reference","text":"quadrant_ranges(sig_quad) -> (ys, xs)\n\nThe signal quadrant as a pair of index ranges. In FFT ordering the negative half of each k axis is exactly the second half of its index range, so the mask is a dense rectangle — which lets the device norm use strided views instead of a boolean mask (a BitMatrix cannot enter a device kernel, and a masked reduction would need a gather).\n\nThrows if the mask is not that rectangle, so a future change to the k-space layout cannot silently corrupt the solver's error control.\n\n\n\n\n\n","title":"ModelPNPS.quadrant_ranges"},{"category":"method","location":"interface/#ModelPNPS.run_scan-Tuple{Any, AbstractVector}","page":"API Reference","text":"run_scan(setup, τs; scan_name, exec, kwargs...) -> Nothing\n\nBuild a Luna.Scans.Scan over the delay array τs and run simulate_delay_point at every τ, calling Output.scansave to write each result into the collected HDF5 file at \"<scan_name>_collected.h5\". The metadata block (combined_grid) is written once on the first scan point.\n\nexec must be a Luna.Scans.AbstractExec instance (e.g. Scans.SlurmExec(...) or Scans.LocalExec()).\n\nzsave selects the propagation snapshots saved at every delay (see simulate_delay_point): an Integer gives a uniform grid of that many points over [0, thickness] (default nz), or a Vector of explicit material thicknesses [m] (e.g. [1e-6, 10e-6, 20e-6, 40e-6]). thickness is appended to the vector if absent so the final slice is always the full-propagation output. The trace datasets become (Nω, nz, Nτ) and the realized z positions are stored once in /grid/zsave. Because the field at an intermediate z equals a dedicated thickness-z run, every shorter thickness comes free from one full-thickness run; note that peak memory scales with the number of z points.\n\nextra_outputs(output_namedtuple) is an optional callable returning extra named tuples to splat into scansave. The default is empty.\n\nKeywords\n\nscan_name: base name of the collected file, \"<scan_name>_collected.h5\".\nexec: the Luna.Scans.AbstractExec instance described above.\nnz = 2, zsave = nz: propagation snapshots, as above.\ninit_dz = 5e-7, rtol = 1e-6, max_dz = 0.0: solver settings forwarded to simulate_delay_point; max_dz = 0.0 means thickness/2. They are recorded in the file's /grid block as provenance.\nnorm = Luna.RK45.weaknorm: RK45 error norm.\nnorm_builder = nothing: a callable setup -> norm, used instead of norm, for a norm that cannot exist before the setup does. Pass norm_builder = signal_quadrant_norm to get signal_quadrant_norm built lazily on the compute node.\ntwin_period = 1: accepted steps between applications of the spectral/temporal windows. 1 applies them after every step, which makes the apodisation damping scale with the step count; a large value applies them only at saves, which with step_on sit at identical positions for any rtol.\nfftw_threads = 0: FFTW threads per process, set where the plans are created so that it reaches procs workers (a top-level Luna.set_fftw_threads does not). With procs workers sharing cpus cores, pass cpus ÷ procs. 0 leaves it alone.\nfftw_mode = :estimate: FFTW planning effort, set on the same path and for the same reason. MEASURE-class planning of production-size 3-D transforms costs tens of minutes per worker.\nstream = true: write the propagation slices to a node-local temp file rather than holding the whole (ω, ky, kx, z) stack in memory (~2.15 GB per slice at production size). Ignored when save-time extraction is active, which stores no slices at all.\nextract_on_save = nothing: reduce each slice as it is produced; see simulate_delay_point. nothing picks the per-device default.\nskip_existing = false: resume an interrupted scan by skipping delay points already present in the collected file. An all-zero slice is the \"not yet computed\" marker, the same test verify_against_collected uses.\n\n\n\n\n\n","title":"ModelPNPS.run_scan"},{"category":"method","location":"interface/#ModelPNPS.run_scan-Tuple{ModelPNPS.TGFROGSetup, AbstractVector}","page":"API Reference","text":"Eager variant: wrap an already-built setup (costs nothing extra when the setup exists anyway, e.g. in interactive use or LocalExec runs).\n\n\n\n\n\n","title":"ModelPNPS.run_scan"},{"category":"method","location":"interface/#ModelPNPS.run_scan-Tuple{NamedTuple, AbstractVector}","page":"API Reference","text":"run_scan(setup_args::NamedTuple, τs; kwargs...)\n\nRECOMMENDED for scan scripts: pass the build_setup keyword arguments as a NamedTuple, e.g.\n\nsetup_args = (; λ0, τfwhm, energy, thickness, material,\n                mask_diam, mask_spacing, λlims, beam, window,\n                R=366.0e-6, N=1024)\nrun_scan(setup_args, τ; ...)\n\nThe setup is then built lazily on each process that executes scan points. This form is robust under EVERY execution mode, including multi-worker (procs > 0) queue scans: a NamedTuple of parameters serialises to the workers by value, whereas a NAMED function defined in a script (make_setup() = ...) serialises by reference and fails to deserialise on workers (Julia ships code only for anonymous closures). The wrapping closure here is defined inside ModelPNPS, which Luna loads on the workers.\n\n\n\n\n\n","title":"ModelPNPS.run_scan"},{"category":"method","location":"interface/#ModelPNPS.signal_quadrant_norm-Tuple{ModelPNPS.TGFROGSetup}","page":"API Reference","text":"signal_quadrant_norm(setup::TGFROGSetup; floor_rel=1e-6)\n\nRegion-relative RK45 error norm for weak-signal accuracy at moderate rtol.\n\nThe default Luna.RK45.weaknorm measures the step error relative to the norm of the WHOLE field, which the three pump beamlets dominate. The FWM signal is orders of magnitude weaker, so the stepper's error budget — concentrated on the fastest-evolving (signal) components — allows a per-step signal error of order rtol × ‖pump‖/‖signal‖ relative to the signal: with rtol = 1e-6 the collected signal carries measured solver errors of 0.1–1% mid-slab, growing to ~10% at 40 µm (see 90_solver_accuracy_test.jl). Brute-forcing rtol = 1e-8 fixes this at ~4× the step count.\n\nThis norm instead measures relative error separately in the signal k-space quadrant (kx < 0, ky < 0 — the same quadrant Iω_full integrates) and in the rest of the field, and returns the larger: rtol then controls the signal's OWN relative error directly, recovering weak-signal accuracy at close to the default-rtol step count.\n\nWhile the signal quadrant is still (nearly) empty its error is measured against a floor of floor_rel × ‖rest‖ (never below atol), so early steps are not throttled by a 0/0 relative error; once the signal exceeds that fraction of the pump field the relative control takes over.\n\nPass the result to simulate_delay_point / run_scan via their norm keyword. Validate a new (rtol, floor_rel) choice against a tight-rtol reference before production use (the pass criterion used here: every z-slice of Iω_win within 1e-3 relative of an rtol = 1e-8 run).\n\n\n\n\n\n","title":"ModelPNPS.signal_quadrant_norm"},{"category":"method","location":"interface/#ModelPNPS.simulate_delay_point-Tuple{ModelPNPS.TGFROGSetup, Real}","page":"API Reference","text":"simulate_delay_point(setup::TGFROGSetup, τi;\n                     nz=2, zsave=nz, init_dz=5e-7, rtol=1e-6, max_dz=0.0,\n                     norm=Luna.RK45.weaknorm, twin_period=1,\n                     filename=nothing, extract_on_save=nothing,\n                     skip_propagation=false)\n    -> NamedTuple\n\nRun the full per-delay computation: apply delay τi to the test beam, coherently superpose the three beamlets, propagate them through the substrate via Luna.run, apply each signal window and extract two spectra per window. The returned NamedTuple has, for a single window, fields (Iω_win, Iω_win_reimaged, Iω_full). For a vector of windows the suffixes recorded in setup.window_suffix are appended (e.g. Iω_win_ωdep, Iω_win_ωdep_reimaged), and the single Iω_full is shared. All extracted arrays have shape (Nω, nz). The returned NamedTuple also carries zsave, the vector of realized z save positions [m] (length nz) — this is metadata, not a per-delay trace, and is excluded from the scansave dataset splat by run_scan.\n\nzsave selects the propagation snapshots. Pass an Integer for a uniform grid of that many points over [0, zmax] (default nz), or a Vector of explicit material thicknesses [m] (e.g. [1e-6, 10e-6, 20e-6, 40e-6]); zmax is appended to the vector if absent. Because the field at an intermediate z equals a dedicated thickness-z run, every shorter thickness comes free from one zmax run. Peak memory scales with nz (the in-memory 4-D field is held per slice).\n\nIω_full is the signal beam collected in full: |E|² integrated over the signal's k-space quadrant only. The propagated field holds the three strong pump beamlets (at the g1/g2/test boxcar corners) plus the weak FWM signal at the fourth corner; integrating over all of k-space would be dominated by the pumps, so we restrict to the quadrant the signal occupies (kx<0, ky<0), which captures the whole signal lobe without aperture vignetting while excluding the pumps. Iω_win ./ Iω_full is therefore the exact per-(ω, τ) collection / chromatic-vignetting efficiency of the signal aperture, so the trace can be corrected for collection vignetting exactly rather than via a power-law approximation. (This assumes the boxcar beams are well separated, so pump tails leaking into the signal quadrant are negligible vs. the signal.)\n\nThe solver keywords go straight to Luna.run: init_dz [m] is the first step, rtol the RK45 relative tolerance, max_dz [m] the step ceiling (0.0 means zmax/2), and norm the error norm — pass signal_quadrant_norm to control the weak signal's OWN relative error rather than the pump-dominated whole field's. twin_period is the number of accepted steps between applications of the spectral/temporal windows; the default 1 applies them on every step, and larger values change the result at the apodisation-leakage level.\n\nextract_on_save reduces each z-slice to its spectra as it is produced, so the field is never stored, streamed or transferred. It defaults to true for a device propagation, where the saved stack costs 14–18 % of the delay point in temp-file traffic, and to false on the host; the two routes are bit-identical, so passing true on the host is safe.\n\nPass filename to persist the propagation to disk: the Luna.run then writes to an Output.HDF5Output at that path instead of an in-memory Output.MemoryOutput. Downstream extraction is identical either way (both outputs index as output[\"Eω\"]/output[\"z\"]); filename is ignored when skip_propagation=true.\n\nSetting skip_propagation=true substitutes the input field for the Luna output, exercising every other code path. This is used by the unit tests to keep the suite fast and deterministic.\n\n\n\n\n\n","title":"ModelPNPS.simulate_delay_point"},{"category":"method","location":"interface/#ModelPNPS.spectral_window!-Tuple{ModelPNPS.InputPulseData, Real, Real}","page":"API Reference","text":"spectral_window!(p::InputPulseData, λmin, λmax;\n                 wfrac_blue=0.05, wfrac_red=0.03) -> p\n\nSmooth tanh band-pass in place: unity well inside (λmin, λmax) [m], rolling off with tanh edges of width wfrac_red · ω(λmax) on the red side and wfrac_blue · ω(λmin) on the blue side. Use before injection to remove content the simulation band does not (or should not) carry — e.g. a residual driver remnant that survived an imperfect spectral filter, which would otherwise dominate the χ³ interaction. The windowed field is the ground truth the retrieval is compared against, so keep the applied window with the run's provenance.\n\n\n\n\n\n","title":"ModelPNPS.spectral_window!"},{"category":"method","location":"interface/#ModelPNPS.verify_against_collected-Tuple{NamedTuple, AbstractString, AbstractVector{<:Integer}}","page":"API Reference","text":"verify_against_collected(setup_args, collected, scanidcs;\n                         zsave, init_dz=5e-7, rtol=1e-6, max_dz=0.0,\n                         norm=Luna.RK45.weaknorm, twin_period=1,\n                         stream=true, extract_on_save=nothing)\n    -> Vector{Dict}\n\nRecompute selected delay points of an existing scan and compare against the collected HDF5 file — the A/B harness for validating a new code path (or a changed grid) against reference data.\n\nFor each scan index in scanidcs, the delay τ is read from /scanvariables/τ in collected, the point is recomputed via simulate_delay_point with the given solver settings (pass the SAME settings the reference scan used, unless deliberately testing a change), and every returned trace dataset (Iω_win, Iω_full, ...) present in the file is compared. Reference points that are still all-zero (not yet computed by a running scan) are reported as NaN and skipped.\n\nReturns one Dict per point with the delay, wall time, Sys.maxrss() [GiB], and for each dataset ks the global relative difference maximum(abs, new - ref)/maximum(abs, ref), plus three diagnostics:\n\nkey meaning\nks max abs difference ÷ this point's reference peak\nks*\"|relscan\" max abs difference ÷ the scan-wide reference peak\nks*\"|refpeak\" this point's reference peak\nks*\"|scanpeak\" the scan-wide reference peak\n\nBoth normalisations matter. A delay-scan wing carries a signal orders of magnitude below the τ≈0 signal, so a difference that is irrelevant in the assembled trace can still be a large fraction of that point's own peak. relscan is what a FROG retrieval sees; the own-peak number is the stricter statement about the code path.\n\nTo test a grid change (e.g. N=640 against an N=1024 reference), pass the changed N inside setup_args — differences then reflect the grid, not the code.\n\nnote: Note\nStrict comparisons need matched FFT configuration: run with the same fftw_threads and fftw_mode as the reference scan (FFT algorithm choice affects round-off). Julia-level threading (JULIA_NUM_THREADS) does NOT affect results and can be used freely to speed up verification.\n\n\n\n\n\n","title":"ModelPNPS.verify_against_collected"},{"category":"section","location":"pnps/#The-PNPS-Framework","page":"PNPS Framework","text":"ModelPNPS is organised around the PNPS (Parametrized Nonlinear Process Spectrum) formalism of Geib et al. (2019)[geib], which provides a single mathematical description for the whole family of self-referenced ultrashort-pulse measurement techniques — FROG, d-scan, MIIPS, time-domain ptychography and their many process variants. How the framework extends when the idealisations behind the standard forward models fail — dispersion in the nonlinear medium, the beam-crossing geometry, the chromatic collection aperture — is the subject of the paper ModelPNPS was built for[travers].\n\n[geib]: N. C. Geib, M. Zilk, T. Pertsch, and F. Eilenberger, \"Common pulse retrieval algorithm: a fast and universal method to retrieve ultrashort pulses,\" Optica 6, 495–505 (2019).\n\n[travers]: J. C. Travers and C. Brahms, \"Extreme ultrashort pulse retrieval with differentiable physical forward models\" (in preparation, 2026). Placeholder — this reference will be updated on publication.","title":"The PNPS Framework"},{"category":"section","location":"pnps/#The-PNPS-trace","page":"PNPS Framework","text":"A PNPS measurement records a two-dimensional trace that depends on the pulse, the (angular) frequency omega, and a method-specific parametrization variable delta:\n\ntildeT(delta omega tildeE) =\n    left mathcalFleft S_deltatildeE(t) right(omega) right^2 \n\nHere tildeE is the complex pulse, mathcalF the Fourier transform, and S_delta the parametrized nonlinear-process signal operator. The signal operator combines\n\na nonlinear process NtildeE that converts the pulse via a collinear nonlinearity, and\na parametrization filter mathcalH_delta(omega) that applies the scanned modification (a delay, a glass insertion, a phase-pattern shift, …).\n\nA measurement technique is therefore specified by a (process × parametrization) pair, and its standard name follows the pattern [Process]-[Parametrization] (e.g. SHG-FROG, SD-FROG, SHG-d-scan).","title":"The PNPS trace"},{"category":"section","location":"pnps/#Nonlinear-processes","page":"PNPS Framework","text":"Process Signal NtildeE Notes\nSHG tildeE^2 second-harmonic generation\nTHG tildeE^3 third-harmonic generation\nSD lverttildeErvert^2tildeE self-diffraction\nPG lverttildeErvert^2tildeE polarization gating\nTG degenerate four-wave mixing transient grating; two gate beams + test\nX- tildeEtildeE_textref cross-correlation with a reference","title":"Nonlinear processes"},{"category":"section","location":"pnps/#Parametrizations","page":"PNPS Framework","text":"Parametrization Variable Filter Family\nDelay delay tau e^i(omega+Omega_0)tau FROG\nGlass insertion thickness z e^i(omega+Omega_0)k(omega)z d-scan\nPattern shift phase-pattern shift e^pm i(omega+Omega_0)delta MIIPS\nPosition spatial/scan position spatial filtering ptychography","title":"Parametrizations"},{"category":"section","location":"pnps/#Roadmap","page":"PNPS Framework","text":"ModelPNPS currently implements TG-FROG. The table below places the planned methods in the PNPS taxonomy and tracks their status. Several SHG/SFG-based methods depend on second-order nonlinearity support arriving in Luna.\n\nTechnique Process Parametrization Status\nTG-FROG TG (four-wave mixing) delay ✅ implemented\nX-TG-FROG TG + reference delay 🔜 planned\nSD-FROG SD delay 🟡 input geometry implemented\nSHG-FROG SHG delay ⏳ pending Luna SHG/SFG support\nTHG-FROG THG delay ⏳ planned\nX-FROG (SHG/SD/THG) cross-correlation delay ⏳ planned\nSHG-d-scan SHG glass insertion ⏳ pending Luna SHG/SFG support\nSD-d-scan SD glass insertion 🔜 planned\nTime-domain ptychography SHG/THG/SD position ⏳ planned\n\nLegend: ✅ available · 🟡 partial · 🔜 planned next · ⏳ planned (may depend on upstream features).\n\nThe modelling goal is the same across every entry: capture the spatial, dispersive, phase-matching, walkoff and nonlinear-efficiency physics of the real experiment, so that the simulated trace is a usable ground truth for retrieval-algorithm development and validation.","title":"Roadmap"},{"category":"section","location":"accuracy/#Accuracy-and-Validation","page":"Accuracy and Validation","text":"A TG-FROG trace is a weak signal sitting next to three strong pumps. Most of this page follows from that fact: the default adaptive stepper does not automatically control the quantity you care about, the apodisation cadence interacts with the step count, and a new code path is trusted by differencing it against reference data. For the approximations the simulation itself makes — representation, boundary, discretisation, interfaces — see the reference paper (The paper), which enumerates and bounds each one for the production instrument model.","title":"Accuracy and Validation"},{"category":"section","location":"accuracy/#The-weak-signal-problem","page":"Accuracy and Validation","text":"Luna's default error norm, RK45.weaknorm, measures the step error relative to the norm of the whole field. In this geometry the three pump beamlets dominate that norm, and the four-wave-mixing signal is orders of magnitude weaker. The stepper's error budget is concentrated on the fastest-evolving components — the signal — so what it actually permits is a per-step signal error of order\n\ntextrtol times fractextpumptextsignal\n\nrelative to the signal. Measured consequence: at rtol = 1e-6 the collected signal carries solver errors of 0.1–1 % mid-slab, growing to about 10 % at 40 µm.\n\nBrute force works — rtol = 1e-8 fixes it — but costs roughly 4× the step count.","title":"The weak-signal problem"},{"category":"section","location":"accuracy/#The-region-relative-norm","page":"Accuracy and Validation","text":"signal_quadrant_norm is the cheaper answer. It measures relative error separately in the signal's k-space quadrant (kx < 0, ky < 0, the same quadrant Iω_full integrates) and in the rest of the field, and returns the larger of the two. rtol then controls the signal's own relative error directly, recovering weak-signal accuracy at close to the default-rtol step count.\n\nsetup = build_setup(; ...)\nout = simulate_delay_point(setup, τ; rtol = 1e-6, norm = signal_quadrant_norm(setup))\n\nWhile the signal quadrant is still nearly empty, its error would be a 0/0 relative quantity and would throttle the early steps. It is therefore measured against a floor of floor_rel × ‖rest‖ (never below atol); once the signal exceeds that fraction of the pump field, relative control takes over. floor_rel defaults to 1e-6.","title":"The region-relative norm"},{"category":"section","location":"accuracy/#Inside-a-scan","page":"Accuracy and Validation","text":"The norm needs the setup to exist, and in a scan the setup is built lazily on the compute node. run_scan therefore takes norm_builder, a callable setup -> norm, instead of a ready-made norm:\n\nrun_scan(setup_args, τ;\n         scan_name = \"my_run\", exec = Scans.LocalExec(),\n         rtol = 1e-7, norm_builder = signal_quadrant_norm)\n\nThe norm that was actually used is recorded in the output file's /grid/error_norm as provenance, alongside rtol and max_dz.\n\nnote: Validate a new `(rtol, floor_rel)` before production\nThe pass criterion used here is: every z-slice of Iω_win within 10⁻³ relative of an rtol = 1e-8 run. Do that once for a new geometry rather than assuming a setting transfers.","title":"Inside a scan"},{"category":"section","location":"accuracy/#Apodisation-cadence:-twin_period","page":"Accuracy and Validation","text":"Luna applies spectral and temporal windows in place during propagation, to absorb what reaches the edges of the computational domain. twin_period sets how many accepted steps pass between applications.\n\ntwin_period = 1 (the default) applies them after every accepted step. The damping then scales with the step count, which makes the scheme non-convergent in rtol: halve the tolerance, take more steps, get more absorption.\nA large value applies them only immediately before a save (Luna always windows before a save). Combined with step_on — which simulate_delay_point passes automatically, so the stepper lands exactly on each zsave position — the windows are then applied at identical positions for any rtol, and a tolerance-convergence study means something.\n\nThe production campaigns use the second, passing a number larger than any achievable step count:\n\nconst TWIN_SAVES_ONLY = 1_000_000_000\nrun_scan(setup_args, τ; ..., twin_period = TWIN_SAVES_ONLY)\n\nThe value is recorded in /grid/twin_period. Note that this changes results at the apodisation-leakage level, so it is a choice to make once per campaign and record, not to vary between runs you intend to compare.","title":"Apodisation cadence: twin_period"},{"category":"section","location":"accuracy/#The-exact-collection-efficiency","page":"Accuracy and Validation","text":"Every run also produces Iω_full: |E|² integrated over the signal's k-space quadrant only. The propagated field holds the three strong pump beamlets plus the weak signal in the fourth corner, so integrating over all of k-space would be dominated by the pumps; restricting to the signal quadrant captures the whole signal lobe without aperture vignetting while excluding them.\n\nIω_win ./ Iω_full is therefore the exact per-(ω, τ) collection and chromatic vignetting efficiency of the signal aperture — so a trace can be corrected for vignetting exactly, rather than through a power-law approximation. This assumes the boxcar beams are well separated, so that pump tails leaking into the signal quadrant are negligible against the signal.\n\nTo inspect it, load the same file with window_key = \"Iω_full\":\n\nnt_win  = load_simulated_scan(\"my_run_collected.h5\")\nnt_full = load_simulated_scan(\"my_run_collected.h5\"; window_key = \"Iω_full\")\nη = vec(sum(nt_win.trace, dims = 2) ./ sum(nt_full.trace, dims = 2))","title":"The exact collection efficiency"},{"category":"section","location":"accuracy/#A/B-validation-against-reference-data","page":"Accuracy and Validation","text":"verify_against_collected is the harness for the question \"did this change alter the physics?\". It recomputes selected delay points of an existing scan and compares them against the stored file, dataset by dataset.\n\nresults = verify_against_collected(setup_args, \"reference_collected.h5\", 1:5;\n                                   zsave = zvec, rtol = 1e-7, max_dz = 2e-6)\n\nFor each index the delay is read from /scanvariables/τ in the reference file, the point is recomputed with the given solver settings — pass the same settings the reference used, unless you are deliberately testing a change — and every trace dataset present in the file is compared. Reference points that are still all-zero, because a scan is running, are reported as NaN and skipped.","title":"A/B validation against reference data"},{"category":"section","location":"accuracy/#Reading-the-result","page":"Accuracy and Validation","text":"One Dict per point, with the delay, wall time, Sys.maxrss() in GiB, and for each dataset four numbers:\n\nKey Meaning\nks max absolute difference ÷ this point's reference peak\nks*\"|relscan\" max absolute difference ÷ the scan-wide reference peak\nks*\"|refpeak\" this point's reference peak\nks*\"|scanpeak\" the scan-wide reference peak\n\nBoth normalisations matter, and they answer different questions. A delay-scan wing carries a signal orders of magnitude below the τ ≈ 0 signal, so a difference that is irrelevant in the assembled trace can still be a large fraction of that point's own peak. relscan is what a FROG retrieval sees; the own-peak number is the stricter statement about the code path.","title":"Reading the result"},{"category":"section","location":"accuracy/#Comparing-across-grid-sizes","page":"Accuracy and Validation","text":"To test a grid change — say N = 640 against an N = 1024 reference — pass the changed N inside setup_args. Differences then reflect the grid, not the code.\n\nThe k-space-integrated spectra (Iω_win, Iω_full, …) are in FFT-bin units that scale as N⁴ at fixed R, by Parseval over the transverse FFT, so the recomputed values are rescaled to the reference grid's units before comparison. The re-imaged (real-space pixel) spectra are N-invariant and are not rescaled.\n\nnote: Match the FFT configuration\nStrict comparisons need the same fftw_threads and fftw_mode as the reference scan: FFT algorithm choice affects round-off. Julia-level threading (JULIA_NUM_THREADS) does not affect results and can be used freely to speed verification up.","title":"Comparing across grid sizes"},{"category":"section","location":"accuracy/#Free-intermediate-thicknesses","page":"Accuracy and Validation","text":"Because the field at an intermediate z equals a dedicated thickness-z run, every shorter substrate thickness comes free from one full-thickness run. Pass a vector of explicit thicknesses as zsave and the trace datasets become (Nω, nz, Nτ), with the realised positions stored once in /grid/zsave.\n\nzsave = [1, 2, 4, 8, 12, 20, 40] .* 1e-6\nrun_scan(setup_args, τ; ..., zsave = zsave)\n\nThis is the cheapest convergence study available: thickness is the parameter the trace is most sensitive to, and this gets a whole ladder of it for the price of one scan. Peak memory scales with the number of z points, since the in-memory 4-D field is held per slice.","title":"Free intermediate thicknesses"},{"category":"section","location":"accuracy/#What-the-test-suite-guarantees","page":"Accuracy and Validation","text":"The test suite is deliberately laptop-fast and does not attempt to validate the physics of a production run. It covers:\n\nevery primitive in isolation — mask construction, window construction, k-space field builders, tilts, delays, extraction;\nsimulate_delay_point(...; skip_propagation = true), which exercises the whole per-delay path except Luna.run itself;\none tiny end-to-end run on a 32×32 grid, which is the only place the integration boundary with Luna is proved;\nthe full device path on JLArrays, so the GPU code paths are covered without a GPU;\ntype stability of the public API through @inferred, and package, import and error-analysis hygiene through Aqua, ExplicitImports and JET.\n\nEverything above that level — solver tolerances, apodisation cadence, grid convergence — is what this page's tools are for.","title":"What the test suite guarantees"},{"category":"section","location":"accuracy/#API","page":"Accuracy and Validation","text":"signal_quadrant_norm and verify_against_collected are documented on the API Reference page; rtol, max_dz, twin_period, norm and zsave under simulate_delay_point and run_scan.","title":"API"},{"category":"section","location":"nonlinear_response/#Nonlinear-Response","page":"Nonlinear Response","text":"The TG-FROG signal is a χ⁽³⁾ four-wave-mixing product, so what build_setup puts on the right-hand side of the propagation equation decides what physics the trace contains. This page describes the two responses available on the default envelope grid — the instantaneous electronic Kerr effect, and the optional delayed nuclear (Raman) contribution — the convention that ties them together, and when the delayed part is worth its cost.\n\nThe third option, propagating a real carrier-resolved field instead of an envelope, changes the representation rather than the response and has its own page: Field-Resolved Mode.","title":"Nonlinear Response"},{"category":"section","location":"nonlinear_response/#The-default:-instantaneous-Kerr","page":"Nonlinear Response","text":"With no extra keywords the response is Luna's Nonlinear.Kerr_env(χ3), the instantaneous electronic nonlinearity of the substrate material, with χ⁽³⁾ taken from Luna.PhysData for the material symbol you pass:\n\nP_textNL = tfrac34varepsilon_0chi^(3)E^2 E\n\nThe 3/4 is the envelope convention: it is what remains of E^3 once the third-harmonic and counter-rotating terms are dropped, which is exactly the approximation an envelope grid makes. Kerr_env applies it internally.\n\nThis is the right response for essentially every production run. Fused silica's electronic response is close to instantaneous on a few-femtosecond timescale, and the trace is dominated by it.","title":"The default: instantaneous Kerr"},{"category":"section","location":"nonlinear_response/#The-delayed-nuclear-response","page":"Nonlinear Response","text":"Set raman = true to add the delayed nuclear contribution. χ⁽³⁾ is then split into an instantaneous electronic part and a delayed part carrying the nuclear fraction f_R:\n\nP_textNL = tfrac34varepsilon_0chi^(3)\n    left(1 - f_R)E^2 E + f_R E(h_R circledast E^2)right\n\nsetup = build_setup(;\n    λ0 = 260e-9, τfwhm = 2e-15, energy = 0.2e-6,\n    thickness = 40e-6, material = :SiO2,\n    mask_diam = 1.0e-3, mask_spacing = 0.5e-3,\n    beam, window,\n    raman = true,                # default raman_fraction = 0.18\n)","title":"The delayed nuclear response"},{"category":"section","location":"nonlinear_response/#The-prefactor-convention","page":"Nonlinear Response","text":"Both terms end with the same prefactor. That is the property that matters, and it is not automatic.\n\nKerr_env applies the envelope factor 3/4 internally, while Luna's Raman response applies 1/2 in its sqr! step. The Raman scale therefore carries the compensating (34)(12) = 32, plus the explicit varepsilon_0 that Kerr_env adds for itself. This is the convention of Luna's prop_gnlse, in which f_R is envelope-defined, and it is what makes the quasi-static (long-pulse) limit of the split response identical to the Kerr-only response.\n\nwarning: Luna's low-level envelope examples differ\nLuna's own low-level silica envelope examples omit the 3/2 and so under-weight Raman by a factor 2/3 relative to this convention. If you compare a ModelPNPS run against one of those, that factor is the difference. The equal-prefactor property here is pinned by a quasi-static consistency test in the test suite, which drives the split response with a long pulse and requires it to reproduce the Kerr-only result.","title":"The prefactor convention"},{"category":"section","location":"nonlinear_response/#raman_fraction","page":"Nonlinear Response","text":"raman_fraction = 0.18 is the Blow–Wood value for silica, in the envelope definition above. Change it only with the convention in mind — a value taken from a paper using a different split will not mean the same thing here.","title":"raman_fraction"},{"category":"section","location":"nonlinear_response/#Which-material","page":"Nonlinear Response","text":"raman = true requires a material with an :intermediate condensed-phase Raman model in Luna.PhysData.raman_parameters. For :SiO2 that is the multimode Hollenbeck–Cantrell response — a 13-mode sum. Any other kind throws an ArgumentError naming the material and the kind it actually has, rather than silently propagating something else.","title":"Which material"},{"category":"section","location":"nonlinear_response/#Implementations:-:batched-and-:frozen","page":"Nonlinear Response","text":"raman_impl selects how the convolution is evaluated. Both compute the same thing and agree to rounding accuracy (~10⁻¹⁵ relative); the difference is entirely cost.\n\n:batched (default) — one pair of batched FFTs per right-hand-side evaluation, covering all transverse points at once.\n:frozen — the legacy per-column route, FrozenRamanPolarEnv, kept for A/B comparison.\n\nThe distinction exists because a free-space grid is a hostile environment for a response written for modal simulations. Luna's own RamanPolarEnv recomputes the time-domain kernel and its FFT on every call — negligible at one call per step in a modal run, dominant here, where the response runs once per transverse point. On a 1024² grid that is of order 10⁶ calls per Runge–Kutta stage, each re-evaluating the 13-mode Hollenbeck–Cantrell sum over the doubled time grid.\n\nFrozenRamanPolarEnv was the first fix: it wraps Luna's response and precomputes the frequency-domain kernel once at construction. Freezing is exact here, because the density is constant and the :intermediate response ignores its density argument entirely. :batched then goes further and removes the per-column loop as well. Prefer the default; reach for :frozen only when you want to check one against the other.","title":"Implementations: :batched and :frozen"},{"category":"section","location":"nonlinear_response/#Cost,-and-whether-you-need-it","page":"Nonlinear Response","text":"The delayed response roughly doubles the work in the nonlinear step and adds buffers on the doubled time grid. For few-femtosecond DUV pulses in a thin substrate the electronic response dominates and raman = false is the right default. Turn it on when the pulse is long enough for the nuclear response to follow it, or when you want to know how much of the trace it accounts for — in which case run both and difference the traces.\n\nThat differencing has been done at production scale. The reference paper (see The paper) ran its 1 fs, 260 nm thickness series twice through the full 3D instrument model, once with the instantaneous Kerr response and once with the multimode Hollenbeck–Cantrell silica response at f_R = 018, all else identical. The delayed response distorts the trace at the 10^-4–10^-3 level over 4–40 µm of fused silica, concentrated in the delay wings as a one-sided vibrational wake; retrieving the Raman-containing traces with an instantaneous forward model shifts the retrieved duration by at most 0.02 fs. At these thicknesses the delayed response is measurable in the trace and negligible in the retrieved pulse.","title":"Cost, and whether you need it"},{"category":"section","location":"nonlinear_response/#Not-available-in-field-mode","page":"Nonlinear Response","text":"raman = true together with field_mode = true throws an ArgumentError. The delayed response itself exists for real fields (Luna.Nonlinear.RamanPolarFieldBatched), but f_R as used here is defined in the envelope convention — including the 3/2 reconciling Kerr_env's internal 3/4 with the Raman kernel's 1/2 — and that factor does not carry over to a carrier-resolved field unexamined. Deriving it needs its own quasi-static consistency test, matching the envelope one. Rather than guess, the combination is refused.","title":"Not available in field mode"},{"category":"section","location":"nonlinear_response/#API","page":"Nonlinear Response","text":"FrozenRamanPolarEnv is documented on the API Reference page; the raman, raman_fraction and raman_impl keywords are documented under build_setup.","title":"API"},{"category":"section","location":"gpu/#Running-on-a-GPU","page":"Running on a GPU","text":"A TG-FROG delay scan is a stack of 3-D nonlinear propagations, and the cost is dominated by 3-D FFTs on a state of shape (Nω, Nky, Nkx). That is exactly the work a GPU is built for, and ModelPNPS runs the whole propagation and extraction path on one by passing a single keyword.\n\nThe speedup, measured on the same geometry, is a factor of about 160:\n\nHardware Per delay point 200-point scan\nNVIDIA H200 42 s ~2.3 h\n2 CPU cores 1.9 h ~16 days\n\nA 200-point scan is a few hours on one card rather than a cluster allocation; the full validation dataset behind the reference paper (see The paper) was generated this way.\n\nwarning: Experimental, and it needs CUDA.jl\nGPU support is working but experimental. Only NVIDIA cards are supported, through CUDA.jl, which is not a dependency of ModelPNPS — add it yourself:import Pkg\nPkg.add(\"CUDA\")The modal-fixed branch of Luna.jl is required too, but that is true of the whole package rather than of the GPU path in particular; see the README's installation section.Everything on this page has been exercised at production scale (it is how the reference paper's validation dataset was generated) and is covered by the device test group, which runs the full device path on JLArrays so that CI tests it without a GPU — but the interface may still change.","title":"Running on a GPU"},{"category":"section","location":"gpu/#Turning-it-on","page":"Running on a GPU","text":"One keyword:\n\nsetup_args = (;\n    λ0 = 260e-9, τfwhm = 1e-15, energy = 0.1e-6,\n    thickness = 40e-6, material = :SiO2,\n    mask_diam = 1.0e-3, mask_spacing = 1.0e-3,\n    λlims = (143e-9, 600e-9),\n    beam, window,\n    arraytype = :cuda,               # <- the whole change\n    beamlets_on_host = true,\n)\n\nrun_scan(setup_args, τ; scan_name = \"my_run\", exec = Scans.LocalExec())\n\nThe rest of this page is about doing this on a real machine without running out of memory or losing an hour to FFT planning.","title":"Turning it on"},{"category":"section","location":"gpu/#Pass-arraytype-inside-setup_args,-not-as-a-run_scan-keyword","page":"Running on a GPU","text":"This is a structural rule, not a style preference.\n\narraytype = :cuda is a symbol, resolved lazily by Luna.resolve_arraytype, which loads CUDA.jl at the moment it is called. Two consequences follow.\n\nThe GPU package must load where the GPU is. run_scan builds the setup lazily, inside the scan closure, on the first point each process executes. Put arraytype in setup_args and CUDA.jl is loaded on the compute node and nowhere else. A cluster login node typically has no GPU at all — and has been observed to fault outright while precompiling CUDA.jl — yet it is where the script is parsed to generate and submit the batch job.\n\nWorld age. Methods defined by a package loaded during a call are invisible to that same call; Julia rejects them as \"too new to be called from this world context\". ModelPNPS handles this internally by resolving the array type first and then re-entering build_setup through Base.invokelatest, which is what _build_setup_resolved exists for. You get this for free by passing the symbol through setup_args.\n\nFor the same reason, a cluster script must not import CUDA at the top level. A rented pod with the GPU directly attached is different: there the simple thing is also the correct one, and a top-level import is fine.","title":"Pass arraytype inside setup_args, not as a run_scan keyword"},{"category":"section","location":"gpu/#beamlets_on_host","page":"Running on a GPU","text":"The three input beamlets are built on the host — masks, Bessel profiles and FFTs are host code — and then moved to the propagation's array type. beamlets_on_host = true keeps them in host memory instead and uploads the delayed sum once per delay point.\n\nThe trade is two fewer resident device fields (about 9 GiB at the production shape) against one host-to-device transfer per point, which is a fraction of a second against a propagation of minutes. Turn it on whenever the card is the constraint, which on a large grid it usually is.","title":"beamlets_on_host"},{"category":"section","location":"gpu/#Budgeting-device-memory","page":"Running on a GPU","text":"Finding out that a shape does not fit by running it costs an hour of rented GPU and a dead process. memory_budget takes the same setup_args NamedTuple that run_scan does, reads only the grid-determining entries, and returns a per-buffer breakdown in GiB. Building the 1-D time grid is the entire cost, so it is free to call.\n\nbu = memory_budget(setup_args)\n@info \"budget\" device = bu.device host = bu.host field = bu.field\n\nThe returned NamedTuple carries the grid sizes (Nω, Nt, Nto, Nωo), the size of one state array (field), the per-buffer terms (state, et_win, eto, ewo, pto, analytic, window, input) and the totals device and host.\n\nThe envelope path obeys a simple rule — nine RK45 registers plus one transform buffer, i.e. ten times the field size, measured exactly on an A40. The field path does not; see Field-Resolved Mode.\n\nwarning: The response's buffer appears on the first step, not at setup\nNonlinear.KerrFieldNoTHG allocates its analytic-signal buffer lazily, when it first sees a field. A card with room to spare after build_setup can therefore still die on the first step — 18 GiB later at the production shape. memory_budget counts it; a measurement taken after build_setup alone will not.","title":"Budgeting device memory"},{"category":"section","location":"gpu/#Guard-the-launch","page":"Running on a GPU","text":"The GPU example script refuses to start rather than dying an hour in, and the pattern is worth copying:\n\nbu = memory_budget(setup_args)\nLuna.resolve_arraytype(:cuda)                 # its own top-level statement\nst = Luna.device_memory_status()              # (free, total) in bytes, or nothing\nif !isnothing(st)\n    free, total = st ./ 2^30\n    @info \"device\" free_GiB = free total_GiB = total\n    bu.device > 0.9 * free && error(\"needs ~$(bu.device) GiB, only $free GiB free\")\nend","title":"Guard the launch"},{"category":"section","location":"gpu/#Measured-shapes","page":"Running on a GPU","text":"Envelope mode, 40 µm fused silica, six collection windows, on an H200 (141 GB):\n\nTransverse N Device Per point\n640 21.9 GiB 17–21 s\n768 ~24 GiB 25–30 s\n900 43.3 GiB 34–41 s\n1024 56.0 GiB 44–53 s\n\nHost peak is around 32 GiB per process at the largest of these — budget system RAM when running several scans concurrently. All of these fit an H200 with better than 2× headroom; N = 1024 also passes on an 80 GB card with roughly 24 GiB of margin, while a 48 GB card cannot run N ≥ 900.\n\nA longer time grid scales this directly: a 305-point scan at N = 900 with Nt 400 runs at ~68 GiB and 55–65 s per point, so about five hours end to end.","title":"Measured shapes"},{"category":"section","location":"gpu/#What-the-GPU-path-does-differently","page":"Running on a GPU","text":"Two behaviours switch on automatically for a device run.\n\nSave-time extraction. Each z-slice is reduced to its spectra as it is produced, so the field is never stored, streamed or transferred. This defaults to true on a device, where the saved stack costs 14–18 % of the delay point in temp-file traffic, and to false on the host. The two routes are bit-identical — the same kernels on the same arrays, minus a lossless HDF5 round trip — so extract_on_save = true is safe on the host too. See simulate_delay_point.\n\nStreaming is skipped. stream = true writes propagation slices to a node-local temp file instead of holding the whole (ω, ky, kx, z) stack in memory. With save-time extraction there are no slices to stream, so no temp file is created.\n\nBetween delay points run_scan runs a garbage collection and calls Luna.device_reclaim(), because a GPU array library keeps its own memory pool that collection alone does not return. Without it the next point can find the card full.","title":"What the GPU path does differently"},{"category":"section","location":"gpu/#Practical-setup-for-a-single-GPU-machine","page":"Running on a GPU","text":"using ModelPNPS\nimport Luna\nimport Luna.Scans\n\nLuna.set_fftw_mode(:estimate)\nLuna.set_fftw_threads(Threads.nthreads())\n\n:estimate planning matters more than it looks: MEASURE-class planning of production-size 3-D transforms takes tens of minutes per worker unless wisdom has been pregenerated for the exact grid and thread count. Set both through run_scan's fftw_threads and fftw_mode keywords as well: they are applied exactly where the plans are created, which is the only place that reaches multi-worker procs scans — a top-level call does not.\n\nUse Scans.LocalExec() on a directly attached card. To put one scan on each GPU of a multi-GPU machine, pin them with the environment and run one process per card:\n\nCUDA_VISIBLE_DEVICES=0 julia --project=. my_scan.jl  # one configuration\nCUDA_VISIBLE_DEVICES=1 julia --project=. my_scan.jl  # another, in parallel\n\nPass skip_existing = true so an interrupted scan resumes where it stopped rather than recomputing points already in the collected file — which matters when the machine is rented by the hour.","title":"Practical setup for a single-GPU machine"},{"category":"section","location":"gpu/#A-dry-run-gate","page":"Running on a GPU","text":"Both example scripts support a dry run: build the configuration, print the budget, check the card, and exit before propagating anything. It costs seconds and catches the mistakes that otherwise surface hours in.\n\nif get(ENV, \"PNPS_DRYRUN\", \"0\") == \"1\"\n    @info \"dry run: configuration validated, NOT propagating\" delays = length(τ)\n    exit(0)\nend","title":"A dry-run gate"},{"category":"section","location":"gpu/#API","page":"Running on a GPU","text":"memory_budget is documented on the API Reference page; the arraytype and beamlets_on_host keywords are documented under build_setup, and extract_on_save, stream, fftw_threads, fftw_mode and skip_existing under run_scan.","title":"API"},{"category":"section","location":"trace_simulation/#Trace-Simulation","page":"Trace Simulation","text":"ModelPNPS generates synthetic TG-FROG (Transient Grating FROG) spectrograms by full spatially-resolved nonlinear propagation through a thin solid medium. Given an analytic input pulse and an experimental geometry, it produces the I(omega tau) trace a real apparatus would record — the ground truth for benchmarking and developing retrieval algorithms.\n\nThe propagation is performed by Luna.jl using its angular-spectrum free-space propagator and instantaneous Kerr χ⁽³⁾ response. A production run needs either a GPU or a SLURM cluster: full delay scans at realistic grid sizes (Nω ≈ 4096, N ≈ 256–1024) take tens of minutes to hours per delay point on CPUs, and well under a minute on a data-centre GPU (see Running on a GPU).\n\nThe complete specification of this instrument model — including bounds on every approximation the simulation makes, all at or below the 10^-4 level on the trace — is given in the reference paper (see The paper), which used ModelPNPS to generate the ground-truth traces for its retrieval validation study.","title":"Trace Simulation"},{"category":"section","location":"trace_simulation/#Physical-model","page":"Trace Simulation","text":"(Image: The folded-boxcar TG-FROG geometry)\n\nThe geometry ModelPNPS models, from the reference paper: (a) the four-hole input mask, with hole diameter D and hole-centre offset d from the symmetry axes; (b) the optical chain — the three beamlets are crossed by a focusing optic inside the thin dispersive substrate, and the diffracted signal leaves through the background-free fourth direction and passes the signal aperture. Inset: the scanned delay τ of the gate pair relative to the probe.\n\nThree input beams (two gates g1, g2 and a delayed test pulse t) are crossed inside a thin nonlinear substrate (e.g. UV fused silica). Their interference produces a transient refractive-index grating; the test pulse diffracts off this grating into the fourth corner of the boxcar via degenerate four-wave mixing,\n\n    mathbfk_textsignal = mathbfk_g2 - mathbfk_g1 + mathbfk_t\n\nThe boxcar layout (looking along +z):\n\n    test (-x, +y)    | gate 1 (+x, +y)\n   --------------------------------------\n    signal (-x, -y)  | gate 2 (+x, -y)\n\nScanning the test-pulse delay τ and spectrally resolving the diffracted signal yields a 2-D I(ω, τ) spectrogram — the TG-FROG trace.\n\nBecause the beams are propagated rather than idealised, the trace contains the instrument effects a real measurement contains. Dispersion in the substrate reshapes the pulse as it generates signal. The crossed beams reach each point of the focus with different relative arrival times, which smears the trace along the delay axis with a width set by the mask geometry and wavelength alone, sigma propto (dD)lambda_0c. And the apertures act as frequency-dependent spatial filters, on the input beamlets (so the pulse that actually gates the interaction is the mask-vignetted beamlet, saved as Iω_beamlet) and on the collected signal. The reference paper (see The paper) derives each effect and measures its size on this simulation.\n\nThe simulation pipeline is:\n\nBuild a temporal/spectral grid (Luna EnvGrid) and a spatial grid (Luna FreeGrid).\nSet up material dispersion (Sellmeier n(λ)) and Kerr χ⁽³⁾ via Luna.\nConstruct the three input beamlets in k-space at the substrate.\nFor each delay τᵢ:\nApply exp(-iωτᵢ) to the test beam.\nCoherently superpose all three beamlets.\nPropagate through the substrate via Luna.run (adaptive RK4(5) split-step), optionally saving snapshots at several thicknesses in the one pass (see Many material thicknesses from one run).\nApply the signal-extraction window in k-space.\nExtract two spectral diagnostics: a re-imaged on-axis spectrum and a fully-integrated spectrum.\nSave all outputs to a single HDF5 file via Luna.Output.scansave.","title":"Physical model"},{"category":"section","location":"trace_simulation/#Two-beam-models","page":"Trace Simulation","text":"Two AbstractInputBeam subtypes are provided:\n\nHE11Beam — the master experimental model. The HE₁₁ mode of a hollow capillary fibre is collimated by a long lens, clipped by a four-hole apodised mask in the collimated beam, then focused into the substrate. Each hole selects one of the four boxcar arms. Chromatic vignetting is captured exactly because the mask plane ↔ k-space mapping is wavelength-dependent.\nGaussianBeam — a simplified Gaussian-beam model that places three Gaussian beams directly at the correct k-space angles (no fibre mode, no physical mask). Useful as a sanity-check baseline.","title":"Two beam models"},{"category":"section","location":"trace_simulation/#Three-signal-extraction-window-types","page":"Trace Simulation","text":"PhysicalMaskWindow — the master experimental signal extraction: a frequency-dependent apodised hole in the mask plane. Apodisation choices: :hard, :supergauss (default order 16), :tanh. Chromatic vignetting is captured exactly.\nPlanckWindow — a smooth, frequency-independent radial Planck taper in k-space. No chromatic vignetting; baseline for the Gaussian model.\nPlanckOmegaWindow — a smooth, frequency-dependent Planck taper that mimics the chromatic vignetting of the physical mask while keeping the smooth-edge advantage. Used to isolate the two effects (smooth-edge vs ω-scaling) within the Gaussian model.\n\nWhen build_setup is given a vector of windows, every per-delay output is computed for each window in turn and saved with a suffix. The standard two-window pattern [PlanckWindow, PlanckOmegaWindow] produces output keys Iω_win, Iω_win_reimaged, Iω_win_ωdep, Iω_win_ωdep_reimaged.","title":"Three signal-extraction window types"},{"category":"section","location":"trace_simulation/#Self-diffraction-geometry","page":"Trace Simulation","text":"The default layout is the four-hole boxcar above, selected by geometry = :tg. geometry = :sd builds a self-diffraction geometry instead: two collinear holes rather than four, on one axis, with the signal at\n\n    mathbfk_textsignal = 2mathbfk_E - mathbfk_G\n\nwhere E is the probe and G the gate. With the two holes at pm s2 (s = mask_spacing + mask_diam, centre to centre) the signal appears at -3s2 — one further slot out, on the probe's side.\n\nsetup = build_setup(;\n    λ0 = 260e-9, τfwhm = 2e-15, energy = 0.2e-6,\n    thickness = 40e-6, material = :SiO2,\n    mask_diam = 1.0e-3, mask_spacing = 0.5e-3,\n    beam = HE11Beam(125e-6, 5.0, 0.1),\n    window = PhysicalMaskWindow(; holex = -1.5 * 1.5e-3, holey = 0.0,\n                                  holediam = 1.0e-3, zmask = 0.1),\n    geometry = :sd,\n)\n\nThe layout is symmetric about the axis — probe at -s2, gate at +s2 — rather than putting the probe on axis and centring the signal. The centred variant needs a 27 % narrower grid, but cuts the two beams from very different parts of the HE₁₁ profile, giving them unequal energy; the SD signal is E^2G^*, so that asymmetry does not cancel. Which beam carries the delay follows the same convention as TG: the gate appears once, carries the conjugation, and takes the delay.\n\ngeometry selects both the beamlet layout and the k-space bound used by optimal_spatial_grid — the SD signal position bounds the nonlinear k-content directly, so unlike the boxcar no extra factor of three is applied. The signal position is written to the output metadata as sd_signal_x, along with sd_separation_cc, so a collection window can be placed from the file.\n\nnote: What is and is not supported\ngeometry = :sd is implemented for HE11Beam only, and build_setup throws an ArgumentError for any other beam model — the Gaussian builder always places three beams at the boxcar corners and would otherwise silently produce a TG field on an SD-sized grid. An unknown geometry is likewise rejected rather than treated as :tg.The windowed outputs (Iω_win, Iω_win_reimaged) are correct for SD provided the window is placed at the SD signal position. The Iω_full diagnostic and signal_quadrant_norm are boxcar-specific: both integrate the k_x  0 k_y  0 quadrant, which is not where the SD signal sits. Treat them as not yet meaningful in this geometry.","title":"Self-diffraction geometry"},{"category":"section","location":"trace_simulation/#Worked-example:-mask-scheme","page":"Trace Simulation","text":"using ModelPNPS\nimport Luna.Scans\n\nbeam   = HE11Beam(125e-6, 5.0, 0.1)        # fibre radius, f_coll, f_foc\nwindow = PhysicalMaskWindow(holex=-0.75e-3, holey=-0.75e-3,\n                            holediam=0.5e-3, zmask=0.1,\n                            apod=:supergauss, apod_param=16)\n\nsetup = build_setup(; λ0=260e-9, τfwhm=2e-15, energy=0.2e-6,\n                      thickness=10e-6, material=:SiO2,\n                      mask_diam=1.0e-3, mask_spacing=0.5e-3,\n                      beam, window)\n\nτ    = collect(range(-10e-15, 10e-15, 80))\nexec = Scans.SlurmExec(@__FILE__, length(τ); memory=\"18G\", arraymode=:batch)\nrun_scan(setup, τ; scan_name=\"my_mask_run\", exec)\n\nSee the [tgfrog_window_series.jl example][slurm-example] for a full production-scale script: the same beam and mask scheme, a ladder of substrate thicknesses from one propagation (zsave), and a series of collection-window diameters recorded simultaneously. The [tgfrog_window_series_gpu.jl example][gpu-example] runs the same measurement on a single GPU.\n\n[slurm-example]: https://github.com/LupoLab/ModelPNPS.jl/blob/main/examples/tgfrogwindowseries.jl [gpu-example]: https://github.com/LupoLab/ModelPNPS.jl/blob/main/examples/tgfrogwindowseries_gpu.jl","title":"Worked example: mask scheme"},{"category":"section","location":"trace_simulation/#Worked-example:-Gaussian-beam-scheme","page":"Trace Simulation","text":"using ModelPNPS\nimport Luna.Scans\n\nf_foc, mask_diam, mask_spacing = 0.1, 1.0e-3, 0.5e-3\nλ0       = 260e-9\nw0       = λ0 * f_foc / (π * mask_diam/2)\nd_hole   = mask_spacing/2 + mask_diam/2\nΔk       = 2π/λ0 * sin(d_hole / f_foc)\n\nbeam    = GaussianBeam(w0, f_foc)\nwindows = [PlanckWindow(kxc=-Δk, kyc=-Δk, kwidth=2.5/w0, pad=1.25),\n           PlanckOmegaWindow(xc=-d_hole, yc=-d_hole,\n                             holediam=mask_diam/2, f_foc=f_foc, pad=1.25)]\n\nsetup = build_setup(; λ0, τfwhm=2e-15, energy=0.2e-6,\n                      thickness=10e-6, material=:SiO2,\n                      mask_diam, mask_spacing,\n                      beam, window=windows)\n\nτ    = collect(range(-10e-15, 10e-15, 80))\nexec = Scans.SlurmExec(@__FILE__, length(τ); memory=\"18G\", arraymode=:batch)\nrun_scan(setup, τ; scan_name=\"my_gaussian_run\", exec)","title":"Worked example: Gaussian-beam scheme"},{"category":"section","location":"trace_simulation/#Spatial-grid-sizing","page":"Trace Simulation","text":"optimal_spatial_grid computes a (R, N) pair such that the spatial grid\n\ncontains at least n_airy Airy patterns from a single mask hole at the longest wavelength λmax (real-space containment),\nresolves the Airy pattern at the shortest wavelength λmin with at least pts_per_lobe real-space samples across the central lobe,\nhas k-space half-extent exceeding the largest FWM nonlinear k-vector at λmin, with a safety headroom factor.\n\nN is rounded up to the next power of 2 for FFT efficiency. For typical DUV parameters (f_foc=0.1 m, mask_diam=1 mm, mask_spacing=0.5 mm, λmin=160 nm, λmax=500 nm) this produces R ≈ 3 mm and N in the range 256–1024.\n\nThe default safety=1.5 is conservative; reducing it produces a smaller grid (and faster simulation) at the cost of potential FWM aliasing.","title":"Spatial grid sizing"},{"category":"section","location":"trace_simulation/#Loading-scan-output","page":"Trace Simulation","text":"run_scan writes one HDF5 file per delay scan via Luna.Output.scansave. load_simulated_scan reads that raw file, extracts the chosen signal window and propagation z-slice, fftshifts the ω-dependent arrays into natural (centred) order, and returns the trace and reference spectra as a NamedTuple ready for inspection, plotting, or custom post-processing (to pull out several material thicknesses at once, see Many material thicknesses from one run):\n\nusing ModelPNPS\n\nnt = load_simulated_scan(\"my_mask_run_collected.h5\";\n                         window_key=\"Iω_win\", z_index=:end)\n# nt.ω, nt.τ, nt.trace (Nω × Nτ), nt.Iω, nt.It, ...\n\nTo retrieve the pulse from a scan, hand the collected file to Croak, the companion retrieval package: it reads these files directly, including the /grid/delay_convention marker that fixes the delay-axis orientation, and compares its result against the stored beamlet ground truth.","title":"Loading scan output"},{"category":"section","location":"trace_simulation/#Many-material-thicknesses-from-one-run","page":"Trace Simulation","text":"A delay scan propagates the field to the full substrate thickness, but the trace at any shorter thickness can be recovered from the same run at essentially no extra cost. The propagation is a forward-marching integrator with z-independent dynamics (constant density, per-unit-length dispersion), so the field captured at an intermediate z is identical to a dedicated run of thickness z. Saving an intermediate snapshot costs only one dense-output interpolation — the expensive integration is unchanged. A single 40 µm run therefore yields the 1, 10, 20 and 40 µm traces together.\n\nSnapshots are selected with the zsave keyword of run_scan (forwarded to simulate_delay_point):\n\n# Explicit material thicknesses [m]. `thickness` (= zmax) is appended\n# automatically if absent, so the final slice is always the full-propagation\n# output. Here thickness=40e-6 in build_setup.\nrun_scan(setup, τ; scan_name=\"my_run\", exec,\n         zsave=[1e-6, 10e-6, 20e-6, 40e-6])\n\n# Or an integer for a uniform grid of that many slices over [0, thickness]:\nrun_scan(setup, τ; scan_name=\"my_run\", exec, zsave=21)\n\nEach per-delay trace dataset (Iω_win, Iω_win_reimaged, Iω_full, and any windowed variants) is then stored with shape (Nω, nz, Nτ), and the realized z positions are written once to /grid/zsave. The default (zsave=2, equivalently the legacy nz=2) keeps the entrance and exit slices only, so existing scripts are unchanged.\n\nload_simulated_scan exposes the saved thicknesses:\n\n# A specific thickness — picks the nearest saved z slice (needs /grid/zsave):\nd10 = load_simulated_scan(\"my_run_collected.h5\"; z_thickness=10e-6)\n# d10.trace is (Nω × Nτ); d10.zsave lists all saved z positions [m].\n\n# Every saved thickness at once — trace becomes (Nω × nz × Nτ):\nallz = load_simulated_scan(\"my_run_collected.h5\"; z_index=:all)\nfor (k, z) in enumerate(allz.zsave)\n    trace_k = allz.trace[:, k, :]      # (Nω × Nτ) at thickness z\nend\n\nz_index=:end (the default) still returns the full-thickness slice as a 2-D (Nω, Nτ) trace, and an integer z_index selects a slice by index. Files written before zsave existed simply have no /grid/zsave and load exactly as before (omitting zsave/z_thickness).\n\nCost is the key advantage: adding snapshots leaves the compute time unchanged. The only overhead is peak memory — the in-memory 4-D field (Nω, Nky, Nkx, nz) is held one slice per saved z — which is comfortable for up to ~20 thicknesses at realistic grid sizes. (A streaming reduction that decouples memory from nz entirely is a possible future extension for hundreds of slices.)","title":"Many material thicknesses from one run"},{"category":"section","location":"trace_simulation/#Diagnostics:-the-retrievable-pulse-and-the-efficiency-curve","page":"Trace Simulation","text":"Two diagnostics are saved on every run to make the simulated trace a clean retrieval benchmark.","title":"Diagnostics: the retrievable pulse and the efficiency curve"},{"category":"section","location":"trace_simulation/#The-retrievable-pulse-—-Iω_beamlet-/-It_beamlet","page":"Trace Simulation","text":"Iω_beamlet is the spatially-integrated spectrum of one input beamlet after the mask — i.e. the input pulse as chromatically vignetted by a single mask hole. It_beamlet is its time-domain intensity (the mask is a real amplitude filter, so the beamlet inherits the input pulse's spectral phase). This is the \"ground-truth\" pulse a retrieval should recover, and is the right thing to plot against a reconstructed |E(t)|² and |E(ω)|² — not the un-vignetted input It/Iω. Both are saved under /grid and returned by load_simulated_scan; Ito_beamlet is the 8× oversampled version sharing the To grid.","title":"The retrievable pulse — Iω_beamlet / It_beamlet"},{"category":"section","location":"trace_simulation/#The-exact-collection-efficiency-—-Iω_full","page":"Trace Simulation","text":"The TG-FROG frequency marginal M(omega) = sum_tau I(omegatau) does not match Iω_beamlet, even after the nonlinear omega^n correction. It is important to separate the two very different reasons why, because only one of them is an efficiency that should be divided out:\n\nNonlinear generation scaling (an efficiency) — the χ⁽³⁾ polarization → radiated-field conversion carries explicit ω factors; the (omega+omega_0)^n power law is only an approximation to it.\nThe marginal is a gate convolution, not the spectrum (not an efficiency). Even for a perfect, unvignetted, single pulse, a TG-FROG marginal is intrinsically\n    M(omega) = tildeE(omega)^2 circledast tildeg(omega)^2\n    qquad g(t) = E(t)^2\nthe pulse spectrum convolved with the transient-grating gate spectrum (a baseband function), which smears and broadens it. All three beamlets are the same vignetted pulse (the three holes are symmetric and the HE₁₁ mode is radial), so this is fully determined by the single pulse Iω_beamlet — it is real FROG physics that the retrieval forward model reproduces, and must not be divided out. Vignetting only sets which pulse (Iω_beamlet) enters the convolution.\nCollection vignetting (an efficiency) — the signal beam is collected through the (chromatic) signal window, multiplying the marginal by a per-ω collection efficiency. Under good imaging (output mask conjugate to the input mask) this is small; Iω_full lets you confirm exactly how small.\n\nFor the Gaussian-beam scheme the efficiency factors (1) and (3) are mild (no input mask; a broad, smooth Planck window), which is why its marginal lines up with the spectrum after the omega^2 correction once the gate convolution is accounted for. For the physical-mask scheme factor (3) and the gate convolution of the vignetted spectrum are both significant.\n\nTo remove factor 3 exactly — with no power-law approximation — every run also saves Iω_full: the signal beam collected in full. The propagated field holds the three strong pump beamlets (at the g1/g2/test boxcar corners) plus the weak FWM signal at the fourth corner, so integrating over all of k-space would just be dominated by the pumps. Instead Iω_full integrates |E|² over the signal's k-space quadrant only (the signal sits alone at kx<0, ky<0 while the three pumps occupy the other quadrants), capturing the whole signal lobe with no aperture vignetting and excluding the pumps. The ratio\n\n    eta_textcollect(omega tau) = fracI_textwin(omegatau)\n                                              I_textfull(omegatau)\n\nis then the exact per-(ω, τ) collection / chromatic-vignetting efficiency of the signal aperture. Divide it out (or use Iω_full directly) to obtain a trace free of collection vignetting; the per-ω efficiency curve is the delay-marginal ratio sum_tau I_textwin  sum_tau I_textfull. (This assumes the boxcar beams are well separated, so pump tails leaking into the signal quadrant are negligible vs. the signal — true for any working TG-FROG geometry.) Load it with:\n\nnt_win  = load_simulated_scan(\"run_collected.h5\"; window_key=\"Iω_win\")\nnt_full = load_simulated_scan(\"run_collected.h5\"; window_key=\"Iω_full\")\nη_collect = vec(sum(nt_win.trace, dims=2) ./ sum(nt_full.trace, dims=2))\n\nTogether, Iω_full removes collection vignetting (factor 3) exactly, while Iω_beamlet/It_beamlet give the actual vignetted pulse under test — so once the nonlinear omega^n scaling (factor 1) is divided out, the only remaining \"offset\" is the gate convolution (factor 2), which is genuine FROG physics the retrieval forward model reproduces rather than an artifact to correct.","title":"The exact collection efficiency — Iω_full"},{"category":"section","location":"trace_simulation/#Computational-cost","page":"Trace Simulation","text":"Per delay point, the propagation cost is dominated by the 3-D FFTs in the split-step solver, so it scales with the transverse grid N and the number of frequency bins.\n\nOn a GPU the whole propagation and extraction path runs on the device, and one delay point at the production shape takes 42 s on an NVIDIA H200 against 1.9 h on two CPU cores — a factor of about 160. A 200-point scan is then a couple of hours on one card rather than a fortnight. This is the recommended way to run a campaign; see Running on a GPU for the keyword, the memory budget and the practical setup.\n\nOn CPUs, a delay point at Nω ≈ 4096 and N ≈ 256–1024 takes tens of minutes to hours on a single core, so a scan needs one task per delay. Use Scans.SlurmExec with arraymode = :batch to dispatch the whole scan as one SLURM array job.\n\nEither way, memory_budget tells you what a configuration will need before you launch it, and zsave gets a whole ladder of substrate thicknesses out of one scan — see Many material thicknesses from one run.","title":"Computational cost"},{"category":"section","location":"trace_simulation/#Testability","page":"Trace Simulation","text":"Every primitive (build_he11_kspace, build_gaussian_kspace, apply_tilt, apply_delay, makemask, build_window, extract_signal_spectra) is independently unit-testable. The test file test/tracesimulation_test.jl exercises each in isolation, plus a simulate_delay_point(...; skip_propagation=true) mode that bypasses Luna.run to test signal extraction without paying the propagation cost. A single tiny end-to-end smoke test runs Luna.run on a 32×32 grid in a few seconds — this is the only place the test suite proves the integration boundary works. The full pipeline is validated by the example scripts on SLURM, not by the unit tests.","title":"Testability"},{"category":"section","location":"trace_simulation/#API-reference","page":"Trace Simulation","text":"Full docstrings for every type and function named above — grouped as beam types, signal-window types, setup/simulation/scan, primitives and scan-loading — are collected on the API Reference page.","title":"API reference"},{"category":"section","location":"#ModelPNPS","page":"Home","text":"ModelPNPS is a Julia package for forward modelling of PNPS (Parametrized Nonlinear Process Spectrum) pulse-characterisation measurements. Given an input pulse and an experimental geometry, it generates the trace a real apparatus would record, by full spatially-resolved nonlinear propagation through the measurement medium using Luna.jl.\n\nThe point is a faithful numerical experiment rather than a fast 1-D approximation: the simulated trace contains the effects that the analytic forward models inside retrieval algorithms neglect. Because the input pulse is known exactly, such traces are ground truth for testing and developing retrieval algorithms. ModelPNPS does the forward modelling only; the retrieval half is its companion package, Croak, which reads the scan files ModelPNPS writes and retrieves the pulse from them.\n\n(Image: Simulated TG-FROG traces of a 1 fs, 260 nm pulse after 9.5, 24 and 40 µm of fused silica)\n\nSimulated TG-FROG traces of a 1 fs, 260 nm pulse after 9.5, 24 and 40 µm of fused silica — the substrate-thickness series of the reference paper below. The transient grating, the phase-matched four-wave-mixing signal, the geometrical delay smearing and the chromatic aperture response all emerge from the propagation; none is imposed.","title":"ModelPNPS"},{"category":"section","location":"#The-paper","page":"Home","text":"ModelPNPS was built for, and is described in:\n\nJ. C. Travers and C. Brahms, Extreme ultrashort pulse retrieval with differentiable physical forward models (in preparation, 2026). (Placeholder — this reference will be updated on publication.)\n\nThe paper uses ModelPNPS as a first-principles virtual TG-FROG instrument: a transform-limited 1 fs pulse at 260 nm illuminates a four-hole boxcar mask, the three transmitted beamlets are focused into a fused silica substrate, and the tilted, spatially separated fields are propagated coherently with full angular-spectrum dispersion, diffraction and the Kerr nonlinearity. The signal is collected through an apertured window in the far field, delay by delay, exactly as a spectrometer would record it. The paper specifies this instrument in detail, bounds every approximation the simulation itself makes (at or below the 10^-4 level, with the delayed Raman response treated separately), and uses the resulting traces to validate retrieval models against the dispersion, beam-geometry and collection physics of few-femtosecond deep-ultraviolet measurements. The retrievals themselves are done with Croak, which implements the paper's differentiable forward models and solvers. If you use ModelPNPS in published work, please cite the paper.","title":"The paper"},{"category":"section","location":"#Scope","page":"Home","text":"The aim is a complete PNPS trace-modelling package — 3D numerical models of the major pulse-characterisation experiments, built so that the simulated trace reflects the real physics of the measurement:\n\nSpatial effects — finite beam size, mode shape, beam overlap and crossing geometry, diffraction, apertures and mask edges.\nPhase-matching — the wavelength- and angle-dependent efficiency of the nonlinear process across the interaction volume.\nDispersion — material dispersion of the nonlinear medium and the associated pulse reshaping during propagation.\nWalkoff — spatial and temporal walkoff between the interacting beams.\nChromatic vignetting — the wavelength-dependent spatial filtering of the signal beam by the collection optics.\nReal nonlinear efficiency — the true χ⁽ⁿ⁾ conversion, not an idealised instantaneous-thin-medium response.\n\nThese effects matter most where the analytic forward models break down: broadband DUV/VUV pulses, thick media, strongly phase-mismatched geometries.","title":"Scope"},{"category":"section","location":"#Current-status","page":"Home","text":"The currently implemented process is TG-FROG (Transient-Grating FROG): a degenerate four-wave-mixing measurement in a thin solid substrate, modelled with two beam schemes (hollow-fibre HE₁₁ mode through a four-hole boxcar mask, or a simplified Gaussian-beam baseline) and a choice of signal-extraction windows. A self-diffraction beam layout is also available, as the input geometry for the planned SD-FROG process. See Trace Simulation for the full description and worked examples, and the PNPS Framework page for the broader taxonomy and roadmap.\n\nBeyond the core forward model, the package can inject a measured or separately simulated pulse in place of the analytic Gaussian (Input Pulses), add the delayed nuclear response (Nonlinear Response), and propagate a real carrier-resolved field instead of an envelope (Field-Resolved Mode) — the last of these being how the envelope approximation itself gets tested at single-cycle durations.\n\nnote: Runs on a GPU, and that is the fast way\nA full delay scan at realistic grid sizes is hours of work per delay point on CPUs. On an NVIDIA GPU the whole propagation runs on the device: 42 s per delay point on an H200 against 1.9 h on two CPU cores, a factor of aboutSee Running on a GPU. The CPU path remains fully supportedand is intended for a SLURM cluster, one task per delay.The unit tests stay laptop-fast either way, by exercising every primitive without the propagation step, plus one tiny end-to-end smoke run.","title":"Current status"},{"category":"section","location":"#Contents","page":"Home","text":"Pages = [\n    \"pnps.md\",\n    \"trace_simulation.md\",\n    \"input_pulses.md\",\n    \"nonlinear_response.md\",\n    \"field_mode.md\",\n    \"gpu.md\",\n    \"accuracy.md\",\n    \"interface.md\",\n]\nDepth = 2","title":"Contents"},{"category":"section","location":"#API-Index","page":"Home","text":"","title":"API Index"},{"category":"section","location":"input_pulses/#Input-Pulses","page":"Input Pulses","text":"By default build_setup constructs its own input field: a transform-limited Gaussian at λ0 with intensity FWHM τfwhm, optionally chirped by the GDD and TOD keywords. That is the right pulse for a controlled numerical experiment, where the point is to retrieve a shape you specified exactly.\n\nIt is the wrong pulse when the question is whether a retrieval works on the light a particular laser actually produces. A measured or separately simulated spectrum — structured, with deep minima, satellites and a spectral phase no polynomial reproduces — stresses a retrieval in ways an analytic Gaussian never does. The input_pulse keyword injects one.\n\nThis page covers the whole path: getting the data in, conditioning it, injecting it, and the two conventions that make the difference between a faithful injection and a silently aliased one.","title":"Input Pulses"},{"category":"section","location":"input_pulses/#The-data-container","page":"Input Pulses","text":"InputPulseData holds a complex spectrum Eω on an absolute, ascending, approximately uniform angular-frequency axis ω in rad/s. Absolute means the physical frequency, not an offset from a carrier: the same convention Luna's grid.ω uses, so no carrier bookkeeping is needed anywhere downstream.\n\nAmplitude units are irrelevant. The beamlet builder rescales the assembled beam to the energy you request, so a spectrum in arbitrary detector counts injects exactly as well as one in V/m.\n\nThe constructor validates what it can: matching lengths, at least eight samples, and an ascending axis. Everything else is checked at the point it matters.\n\nusing ModelPNPS\n\nω = collect(range(2π * 3e14, 2π * 2.1e15, 4096))     # rad/s, ascending\nEω = my_complex_spectrum(ω)                          # any complex amplitude scale\npulse = InputPulseData(ω, Eω)","title":"The data container"},{"category":"section","location":"input_pulses/#Loading-from-HDF5","page":"Input Pulses","text":"load_input_pulse reads the two datasets from an HDF5 file. The spectrum must be a native complex dataset — as written by HDF5.jl, or by h5py with a compound ('r', 'i') dtype.\n\npulse = load_input_pulse(\"DUV_opt.h5\"; ω_key = \"ω\", Eω_key = \"filteredEω\")\n\nThe key names default to \"ω\" and \"Eω\"; pass them explicitly when the file uses its own names, as above. A file whose entries are groups rather than datasets throws an ArgumentError naming the offending key, rather than failing later with a type error deep inside the interpolation.","title":"Loading from HDF5"},{"category":"section","location":"input_pulses/#Conditioning:-window,-then-centre","page":"Input Pulses","text":"Two in-place steps normally sit between loading and injection, and the order matters.","title":"Conditioning: window, then centre"},{"category":"section","location":"input_pulses/#1.-Window-out-what-the-experiment-would-not-deliver","page":"Input Pulses","text":"spectral_window! applies a smooth tanh band-pass in place: unity well inside (λmin, λmax), rolling off with edge widths set as a fraction of the edge angular frequency.\n\nspectral_window!(pulse, 148e-9, 830e-9; wfrac_blue = 0.05, wfrac_red = 0.03)\n\nThis step is physical, not cosmetic. A DUV pulse produced by resonant dispersive-wave emission in a hollow fibre arrives alongside a driver remnant that can carry most of the energy — per-ω comparable to the UV peak even when a per-λ plot makes it look negligible, and delayed by hundreds of femtoseconds. Injected raw, it would dominate the χ⁽³⁾ interaction and you would be simulating a different experiment. The window models the separation a real UV beamline provides.\n\nimportant: The windowed pulse is the ground truth\nWhatever survives the window is the pulse the retrieval must be compared against, not the raw file. Keep the window parameters with the run's provenance — extra_grid_metadata is the place for them, and they then travel inside the output file:extra_grid_metadata = Dict{String, Any}(\n    \"source_file\"   => basename(path),\n    \"window_nm\"     => [148.0, 830.0],\n    \"window_wfrac\"  => [0.05, 0.03],\n)","title":"1. Window out what the experiment would not deliver"},{"category":"section","location":"input_pulses/#2.-Centre-the-pulse-on-its-own-time-origin","page":"Input Pulses","text":"center_pulse! removes the linear component of the spectral phase, so the temporal envelope peaks at the data FFT's natural origin. It returns the pulse and the shift applied, in seconds (positive means the pulse arrived late and was advanced).\n\npulse, tshift = center_pulse!(pulse)\n\nA pure linear spectral phase is physically irrelevant — it is a choice of time origin. Numerically it matters twice:\n\nIt sets how much trange the simulation must hold. The window has to cover the pulse and the delay scan; a pulse sitting 500 fs off-origin wastes that span.\nMore importantly, it decides whether the spectrum can be interpolated at all. A pulse far from its grid's time origin has a spectral phase rotating by up to π per sample, and no real/imaginary interpolation can resample that. interp_input_pulse measures the median per-sample phase rotation and warns when it exceeds 1 rad, naming center_pulse! as the fix.\n\ncenter_pulse! requires an approximately uniform ω grid and throws an ArgumentError otherwise. The returned shift is reported modulo the data grid's time period, since the on-grid phase is identical for any branch.","title":"2. Centre the pulse on its own time origin"},{"category":"section","location":"input_pulses/#Injection","page":"Input Pulses","text":"Pass the conditioned pulse to build_setup:\n\nbeam   = HE11Beam(125e-6, 5.0, 0.1)\nwindow = PhysicalMaskWindow(; holex = -0.75e-3, holey = -0.75e-3,\n                              holediam = 1.0e-3, zmask = 0.1, apod = :tanh)\n\nsetup = build_setup(;\n    λ0 = 260e-9, τfwhm = 1e-15, energy = 0.1e-6,\n    thickness = 40e-6, material = :SiO2,\n    mask_diam = 1.0e-3, mask_spacing = 1.0e-3,\n    λlims = (140e-9, 950e-9), trange = 160e-15,\n    beam, window,\n    input_pulse = pulse,\n)\n\nWhat the other keywords mean once input_pulse is set:\n\nKeyword Role with a data pulse\nλ0 nominal only — mask apodisation defaults, diagnostics, metadata\nτfwhm nominal only — no longer describes the injected pulse\nenergy still authoritative; the beam is rescaled to it\nGDD, TOD compose on top of the data phase; leave at zero unless deliberate\nλlims must bracket the data, including its tails — see below","title":"Injection"},{"category":"section","location":"input_pulses/#HE-only,-and-why","page":"Input Pulses","text":"input_pulse is supported for HE11Beam and throws an ArgumentError for any other beam model. This is a real restriction, not an oversight. In the HE₁₁ path the 1-D reference spectrum is the pulse: the chromatic mask vignetting is applied to it downstream, so an arbitrary field composes through the geometry exactly. The GaussianBeam builder instead constructs its own spatio-temporal Gaussian from (λ0, τfwhm) and would silently ignore the data.","title":"HE₁₁ only, and why"},{"category":"section","location":"input_pulses/#Interpolation-onto-the-simulation-grid","page":"Input Pulses","text":"interp_input_pulse resamples the data onto grid.ω with separate cubic B-splines for the real and imaginary parts, zero outside the data's range. Two diagnostics guard the two ways this goes wrong:\n\nToo coarse. If the data's dω is larger than the grid's, the interpolation is inventing spectral detail between samples, and a warning says so. The fix is denser data, not a coarser grid.\nOff-origin. The per-sample phase-rotation warning described above.\n\nFinally the field is shifted to the middle sample of Luna's centred time grid, matching Luna's own Fields.DataField convention, so the propagated beamlets and the stored temporal diagnostics agree.","title":"Interpolation onto the simulation grid"},{"category":"section","location":"input_pulses/#Choosing-λlims-and-trange","page":"Input Pulses","text":"The grid must hold the pulse's tails, not just its core. A red tail clipped at the window edge reappears at the z-saves as wrap-around. Size the window from where the spectrum actually falls below the level you care about (say 10⁻³ of peak), not from the nominal bandwidth, and give trange room for the centred pulse plus the full delay scan.","title":"Choosing λlims and trange"},{"category":"section","location":"input_pulses/#Worked-sequence","page":"Input Pulses","text":"The whole path, with the QA step that catches a bad injection before hours of propagation:\n\nusing ModelPNPS\nimport FFTW\nimport Printf: @printf\n\npulse = load_input_pulse(\"DUV_opt.h5\"; Eω_key = \"filteredEω\")\nspectral_window!(pulse, 148e-9, 830e-9; wfrac_blue = 0.05, wfrac_red = 0.03)\npulse, tshift = center_pulse!(pulse)\n\n# QA on the data's own grid: duration, and how much energy sits inside the\n# temporal window the simulation will hold.\nlet n = nextpow(2, 4 * length(pulse.ω))\n    buf = zeros(ComplexF64, n)\n    buf[eachindex(pulse.ω)] .= pulse.Eω\n    it = abs2.(FFTW.ifft(buf))\n    dt = 2π / (n * (pulse.ω[2] - pulse.ω[1]))\n    t = (0:(n - 1)) .* dt\n    t = ifelse.(t .> n * dt / 2, t .- n * dt, t)\n    fwhm = count(>=(0.5maximum(it)), it) * dt\n    core = sum(it[abs.(t) .< 30e-15]) / sum(it)\n    @printf(\"centred by %+.2f fs, FWHM %.3f fs, %.4f of energy within ±30 fs\\n\",\n            tshift * 1e15, fwhm * 1e15, core)\nend\n\nIf that last fraction is not very close to 1, trange is about to be too small, or the window has left something in the file that does not belong in the simulation.","title":"Worked sequence"},{"category":"section","location":"input_pulses/#Example:-a-single-cycle-RDW-pulse","page":"Input Pulses","text":"The final validation of the reference paper (see The paper) runs exactly the sequence above. The resonant-dispersive-wave emission of a gas-filled hollow-capillary fibre source — a 1.06 fs pulse with a structured spectrum and a train of trailing satellites — is simulated at the generation stage, conditioned with spectral_window! (148–830 nm) and center_pulse!, and injected into the virtual TG-FROG instrument with input_pulse, exactly as a real measurement would receive it. The trace below was recorded at a 9.5 µm fused silica substrate through a 2.0 mm collection hole; because the injected field is known exactly, retrievals of this trace can be scored against the truth.\n\n(Image: Simulated TG-FROG trace of a single-cycle RDW pulse, and the injected pulse itself)\n\nLeft: the simulated TG-FROG trace of the RDW pulse at a 9.5 µm fused silica substrate. Right: the temporal intensity of the injected pulse — a 1.06 fs main peak with trailing satellites from the soliton dynamics of the source.","title":"Example: a single-cycle RDW pulse"},{"category":"section","location":"input_pulses/#API","page":"Input Pulses","text":"Full docstrings for InputPulseData, load_input_pulse, spectral_window!, center_pulse! and interp_input_pulse are on the API Reference page.","title":"API"}]
}
